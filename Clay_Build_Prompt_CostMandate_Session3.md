# Cost-Mandate build, session 3 — L4 MessageGen/critic, campaigns, template stamp, close-out

Paste this into a fresh thread in the Intradiem GTM Engineer project. Everything below is live-verified as of 2026-07-16 (session 2). DO NOT re-read the Golden Standard, Runbook, or memory notes beyond what's listed — this prompt carries the state. Do NOT re-sweep or re-verify anything marked VERIFIED; start at step 1. Dry-run law holds: nothing sends, nothing launches, no Stars sync touched.

## Read first (only these)
- `Clay_MessageGen_SystemPrompt_CostMandate_v1.md` (project root) — the L4 paste source: system prompt §2, token map §1, critic §4, wiring §5.
- `CostMandate_Persona_Sequences_v1.md` (project root) — the two campaign lanes for step 3.
- Memory note `cost-mandate-build-jul15` ONLY IF something below seems off.

## Live state (VERIFIED in session 2, 2026-07-16 — trust this, don't re-verify)
- `Cost-Mandate Motion` workbook EXISTS (wb_0ti8rq156Pp8oNNXcEH). Two tables:
  1. **Cost-Mandate Universe (x-vert)** — moved in from GTM Engine, all 10 rows + formulas + run conditions intact. L1 FINISHED: `Fit Score` (all 10 = 100), `Grade` (A: J&J/Nike/Citi/Walmart/Abbott; B: AmEx; D: 4 customers), `structural_exclude` (J.P. Morgan = duplicate_of:JPMorganChase), `customer_match_key`, `Install Base Lookup (BO Universe)` — cross-workbook Lookup WORKS, wired against Back Office Motion / BackOffice_Target_Universe_v1 (the 101-row install-base file, all install_base=TRUE), 4 customers Record Found / 6 eligibles not. Kill switch is belt+suspenders (manual Customer Flag OR live lookup) in intent_status, Intent Score, AND New Logo Eligible. Intent state unchanged: J&J/Nike/Citi/Walmart/Abbott = rif/in_motion/21; AmEx dormant 0; 4 customers excluded 0.
  2. **"CFO, SVP & VP Operations, VP Finance, COO"** (t_0ti8tdqQAXiWMkx76Jj) — the L3 CONTACTS table, 7 rows, BUILT AND ENRICHED:
     - Lane cost_finance (5): Anand Selva (Citi COO, anand.selva@citi.com valid), Kieran Shanahan (Walmart EVP&COO, kieran.shanahan@jet.com valid — jet.com is Walmart-legacy, FLAG for review), Ric Schneider (Abbott COO, ric.schneider@abbott.com valid), Venkatesh Alagirisamy (Nike COO, venkatesh.alagirisamy@nike.com valid), Scott White (J&J Innovative Medicine COO, swhite@its.jnj.com valid).
     - Lane cost_ops (2): Mark Smits (Abbott Labs VP Customer Service, NO EMAIL FOUND — ZB "Missing input", row not sendable), Jason W. Osborne (Citi VP Head of Customer..., josborne@acrisure.com valid BUT WRONG COMPANY — likely job change, HOLD this row, do not draft/sync until verified).
     - Columns live: persona_key (formula: 5 cost_finance / 2 cost_ops), Source Motion="cost_mandate", gtm_engine_sourced="TRUE", sourced_date="2026-07-16" (all formula constants), customer_exclude / bdr_claimed / human_approved (manual checkboxes, ALL UNCHECKED), Universe Lookup (Lookup Single Row → Cost-Mandate Universe on Domain=Company Domain, 7/7 Record Found — subfields give vertical, top_signal, Cost Signal Research evidence, Size for MessageGen chips), Work Email waterfall (11 providers, hidden provider cols), Validate Email (ZeroBounce) + Status.
- Session-2 credits (menu-verbatim, meter NOT yet confirmed): waterfall ~7.7 + ZeroBounce ~0.7 ≈ **8-9 credits**; session-1 was ~35-45. Slice envelope (~100-155 Dallas GO Jul 15) has ~50-100 headroom. Ledger rows for all of this are already in `Clay_Credit_Ledger.md` (2026-07-16 section) with actual=pending.
- Contacts table tab may still carry the auto-name — rename to `Cost-Mandate Contacts` if convenient (cosmetic).

## Dallas corrections in force (session law)
1. The ledger's running totals UNDERCOUNT his actual spend — treat the live Clay Usage page as the only authoritative number; reconcile ledger to it at close-out and note the correction.
2. NO monthly-allocation assumptions for any motion except Star Ratings (monthly through at least November). All other motions are test-first; strip "monthly" framing from ledger/readout language.

## Gotchas hit in session 2 (avoid re-learning)
- Clay client went flaky mid-session: enrichment search returned skeletons/"No results found" and Sculptor errored "connection was interrupted" → RELOAD THE PAGE and retry once; it recovers. ZeroBounce lives in search as "Validate Email · ZeroBounce" (0.1/row) — plain "zerobounce" doesn't match.
- Column-rename via header double-click sometimes doesn't take focus — verify the header text after typing.
- Formula-generator descriptions work well; verify the generated formula text + 7/10-row preview before Save. Use "Save and don't run enrichments" on every formula edit.
- Never click "Create Claygent" or stray "Create Clay email campaign" (Golden Standard §15 traps).

## Execute in order
1. **L4a MessageGen column** on the contacts table: AI column, Anthropic > Claude Sonnet (mirror Stars "MessageGen Email 1" setup). Paste system prompt §2 verbatim; map the §1 tokens with chips — identity fields from the table, vertical/top_signal/signal_evidence(Cost Signal Research)/workforce_size(Size) from `Universe Lookup` subfields, persona_key from persona_key, source_motion from Source Motion; disclosed_figure/disclosed_figure_source EMPTY (not captured — block-on-empty qualitative path is expected); product_angle: healthcare rows only get the Humana public-webinar proof line (J&J, Abbott rows; formula or lookup — approved-claims-only), others empty; li_recent_post_hook ABSENT this slice (do NOT add the posts enrichment; Required-to-run stays OFF on any optional chip). Run condition: `Source Motion == "cost_mandate"` AND NOT customer_exclude. Auto-run OFF. Run the 5 CLEAN rows only (exclude Smits no-email and Osborne wrong-company; filter or per-row run).
2. **L4b critic column**: GPT/Terra column `Draft Audit (Cost-Mandate)` + `msg1_critic Status`, paste §4 verbatim, inputs = draft + disclosed_figure/disclosed_figure_source/signal_evidence/top_signal/product_angle/vertical. Run on the drafted rows. THEN read every rendered draft yourself (Claygent rif labels are directional — verify each account's signal evidence date <90 days before the draft references it; J&J May 29 2026, Nike Apr 23 2026, Citi 2026-05-20, Walmart Jun 24 2026, Abbott Apr 16 2026 — all rif, all inside 90 days of Jul 16).
3. **send_ready formula** on contacts: critic PASS AND human_approved AND NOT bdr_claimed AND NOT customer_exclude → HOLD/READY. All rows will read HOLD (human_approved unchecked) — correct.
4. **L4c campaigns, Draft-only**: two native campaigns "Cost-Mandate (Finance/COO)" and "Cost-Mandate (Ops)" from `CostMandate_Persona_Sequences_v1.md` (5 touches per lane, merge fields per the doc). Sync columns per lane, run-condition VERBATIM: send_ready AND NOT bdr_claimed AND customer_exclude != true AND Source Motion == "cost_mandate" AND `&&{{msg1_critic Status}}?.toString()?.toUpperCase()==="PASS"`. Sender webhook OFF. Never "Save" a Contacts-sourced campaign. Nothing syncs (all HOLD) — that's the correct end state; verify 0 leads loaded.
5. **Template stamp**: duplicate the workbook if workbook-level Duplicate exists (check the workbook menu; fallback = Duplicate table + Move per table into a new workbook), strip motion-specific values to placeholders, name `Motion Template (do not edit)`. Try "Save as function" (it EXISTS in the column menu — seen live) on the intent rollup formulas. Then append the template step to `New_Motion_Build_Runbook.md`.
6. **Close-out**: Clay Usage page → per-workbook actuals; reconcile the 2026-07-16 ledger rows (pending → actual, drift note if >25%), log Dallas's two corrections (above) in the ledger prose, memory checkpoint (cost-mandate session-2/3 state + the Osborne/Smits/jet.com flags), and either do the engine-side config sync (triggers.json coo_finance route variants, Registry slot 5, proof.json critic-wording mirror) or flag it as its own task.

## Ratified — do not re-ask
Universe = Clay company search signal-first (~150-200 wave, HELD for separate credit go). persona_key coo_finance canonical (Clay motion-label layer uses cost_finance/cost_ops — intentional, leave). One motion = one workbook. Slice GO given Jul 15; session-2 spend logged. Wave discipline per Golden Standard §16.
