# Claude Design — Re-skin Prompt (apply the Intradiem brand kit)
### Paste this into your existing "GTM Engineer Operating System" Design project to re-brand it from your personal blurple to Intradiem.

---

Re-skin this entire design into the Intradiem brand. Keep all content, structure, and interactivity exactly as-is — only change the visual styling. Apply the Intradiem brand kit below consistently across every element (header, tabs, cards, tables, callouts, timeline, footer).

**Palette (use these exactly):**
- Primary accent — Intradiem Orange `#FE5000` (highlights, key numbers, links, active tab, the conviction-callout edge, bullet/arrow accents). Use it sparingly — the spark, not the field.
- Orange deep `#D8400A` (hover/pressed); Orange light `#FF8A5C` (highlighted phrases on dark); Orange tint `#FFE9DF` (soft accent fills, pills).
- Ink `#14181F` (header/cover background, section bars); Ink-2 `#2A323C` (gradient partner, dark dividers).
- Text `#1B1F26` on light; Paper `#FAF8F6` page background; White `#FFFFFF` cards.
- Borders `#EFEBE6` / `#DED8D0`; alternating table rows `#FBF3EE` / `#F7E7DC`; supporting gray `#5A5F66`.
- Secondary data accent (for any second series) `#C24A12`.
- Keep status colors separate and unchanged: green `#2f8f4e`, amber `#b5751a`, red `#c0392b`.

**Type:** Headings/numbers/labels in Outfit (700–800); body in Inter (400/700).

**Design intent:**
- Dark **ink** header with a thin orange accent stripe at the very top; warm **paper** interiors that breathe.
- Orange should cover ~10% of any view — the highlight and the number, never the background.
- Replace every blurple/purple/creme/seafoam value with its Intradiem equivalent above. No purple should remain anywhere.
- Preserve the whimsical-but-serious feel: disciplined dark structure + decisive orange spark, generous whitespace, 2–3 small "discovered" details.
- Keep it print-clean for PDF export (no orphaned headings, no large whitespace gaps).

Then show me the updated version so I can refine.
