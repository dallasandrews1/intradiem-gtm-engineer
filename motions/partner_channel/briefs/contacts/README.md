# Contacts, free path (Sep 1 2026)

Pulled with Clay `find-and-enrich-contacts-at-company` (title-keyword filters, no data points requested): 0 credits. Two calls per account (executive layer, then operations / WFM / shared services / automation layer). Fields are as the index returned them; `role_guess` is a labeled best guess for the buying committee, sales validates.

Not a freshness check: anything that goes on a page for Frank or into a sequence gets a live look (Sales Nav badge, or a paid Enrich Person run with an explicit go). Emails are not on this path; the email waterfall costs credits and runs only on the picked cohort after Frank's go.
