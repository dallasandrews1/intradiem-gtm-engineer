# Partner channel (Haresh Gangwani, Frank Ciccone)

Campaigns 6 and 7 on the operating map: partner-registered accounts worked by GTM Engineering, 3xG first, Five9 next. Source: channel campaigns call Aug 31 2026 (Otter ZblCEtsPmbbMhP33wIjI7mNU7i4) and Naveen's Aug 28 "UPT Attack Accounts" thread.

| File | What it is |
|---|---|
| `Partner_Pilot_Design_Sep1.html` | The pilot design one-pager (Intradiem kit, Present mode built in). Built from `.src.html` by `build_page.py`. Live at https://gtm-partner-pilot.pages.dev (Cloudflare Pages project `gtm-partner-pilot`, deploy folder `~/Desktop/Intradiem Deliverables/deploy-partner-pilot/`; redeploy: copy the built html to `index.html` there, then `CLOUDFLARE_ACCOUNT_ID=37eeacfb7a4767c44ee8f40243b62c96 npx wrangler pages deploy . --project-name=gtm-partner-pilot --branch=main --commit-dirty=true`). |
| `intake_3xg_whitespace.py` | Read-only intake fitted to Frank's 3xG workbook (partner-only sheet + direct back-office block, with the 1,297-account matrix joined for WFM footprint): buckets accounts the way Frank described them, strips every dollar and per-module column, holds current customers and partners, ranks. Writes the candidate list, an extended pool, a report and a scrub manifest. `--selftest` proves the pipe on a synthetic workbook. |
| `ref/sf_customer_partner_segment_<date>.csv` | Read-only snapshot of the Salesforce Customer-or-Partner Audiences segment (`audseg_0tk324emMVGAwsna7g4`), one of the gate sources. Re-pull before each cohort pick. |
| `List_Review_Frank_Sep1.html` | The call deliverable for the Sep 1 review with Frank: every ranked account with bucket, seats, DPA seats, WFM, reseller; filter by bucket, mark cohort / holdout / skip on the row (picks persist in the browser), answer boxes for the four questions, Copy picks puts the decision on the clipboard. Generated by `build_list_review.py` from `review_sep1/`, then `build_page.py`. Local file only, 3xG data, never deployed. |
| `review_sep1/` | Copy of the Sep 1 run outputs (ranked candidates, extended pool, report, scrub manifest) for the list review with Frank. |
| `intake_sf_prepipeline.py` | Read-only intake for the Salesforce Partner Pre-Pipeline printable view (Frank shares the object as a Lightning list view; the PDF of the printable view is the export). Parses the 249 real records (test rows dropped), normalizes the partner, splits the glued owner columns, and writes `sf_prepipeline.csv` plus `sf_prepipeline_baseline.md` (funnel by stage and partner, nurture pool, overlap with the 3xG ranked list graded MATCH / CHECK, what the view cannot carry). Inbox: `automation/inbox/partners/sf_prepipeline/`, outputs in `run_<date>/`. |
| `build_briefs.py` + `briefs/` | The account briefing engine, dry-run on Ally Financial and Maximus Sep 1 2026. One JSON per account in `briefs/data/`, two pages per account (internal, partner-safe) with the partner-safe rules enforced in code, plus `Compare_Briefs_<date>.html` against Jeremy Roderick's Gemini brief. Raw research in `briefs/research/`, Clay free-path contacts in `briefs/contacts/`. See `briefs/README.md`. Local only. |
| `build_landscape.py` + `landscape/` | The EMEA channel partner map (Frank's Sep 2 DM ask): who brings Five9, Cisco, Avaya, Genesys, Alvaria/Aspect, Verint and Amazon Connect to market in EMEA, what each partner carries, and the share data that is actually published. Research fan-out (one pass per platform + one for share and distributors) lands in `landscape/research/`, `merge_landscape.py` normalises it into `landscape/data/emea.json`, `build_landscape.py` writes `landscape/EMEA_Channel_Partner_Map.html` (filterable) + the CSV, and refreshes the `partner-channel-landscape` skill template so Frank's copy never drifts. Deployed at https://gtm-partner-pilot.pages.dev/emea-partners/ (public sources only, nothing 3xG). |
| `Reply_Naveen_UPT_Attack_Accounts_Draft.md` | The reply to Naveen's Aug 28 thread, ready to paste into a reply-all (the Outlook connector cannot create drafts). Not sent. |

Inbox for the workbook: `~/Claude/Projects/Intradiem GTM Engineer/automation/inbox/partners/3xg/` (save the xlsx from Frank's Aug 31 forward there; the script picks the newest `.xlsx`). Outputs land in `run_<date>/` next to it: `3xg_candidates_ranked.csv`, `3xg_extended_pool.csv`, `3xg_intake_report.md`, `3xg_scrub_manifest.json`.

Rules this motion inherits: customer-exclusion gate on every account (current customers are held for the AM, never worked cold), verified-claims gate on every line of copy, human review before any send, nothing DMs Dallas on its own (jobs write logs the daily rundown reads).
