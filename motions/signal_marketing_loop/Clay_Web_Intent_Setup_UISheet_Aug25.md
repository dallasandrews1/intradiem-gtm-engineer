# Clay Website Intent: setup sheet (your hands, then Carter or Sierra's)

Written Aug 25 2026. CORRECTED Aug 25 after Dallas walked the live UI: the flow starts from the table, not from Settings; the docs lag the product. Grounded in Clay's docs at university.clay.com/docs/website-tracking (read Aug 25) and a live read of intradiem.com (WordPress 7.0.4, Google Tag Manager container `GTM-WSDB2RL` on every page, Pardot forms). Labels quoted from the docs; anything marked (confirm) is a label I could not verify and you should read off the screen.

The tracking snippet is generated by the website connection the table is bound to; it is not in the table itself. Part 1 says where to find it. Everything below is in order; do not skip ahead.


## Part 1: what Dallas did (verified by CLI, Aug 25 11:04 UTC)

Done in the Clay UI: workbook `Web intent` (`wb_0tkbo3sbAuHXMbNAwpw`) with table `Event: Web intent Table` (`t_0tkbo3tEWnp4abaaaTb`), configured as **new row per visit**, no intent filters or filter groups. Columns: `Event: Web intent` (source, `f_0tkbo3trQpFPEFW4dZ3`, value = object with `.domain` and `.websiteEvents[].path`), `Company Domain` (`f_0tkbo3tba84UDcMjt9i`), `Enrich Company` (action, credit-consuming, auto-run), `Unique Visited Pages` (formula), `Created At`, `Updated At`. Status "Waiting for website visitors": the table is bound to a connection and no visits have arrived because the tracking script is not on the site yet. The table id is in `automation/config/heat_loop.json` and the scorer reads this exact shape.

Still to do in Clay, from that table:
1. Get the tracking snippet. Two places it can live, per Clay's docs and the table you have: the settings icon on the `Event: Web intent` column header (the source's own settings), or the account menu, `Settings`, `Website tracking`, the connection this table uses, `Install tracking snippet`. Copy it.
2. Credit limit on that same connection: set 500 for the 14-day read. De-anonymization: the Clay-recommended (waterfall) option, not best-match.
3. `Enrich Company` column: turn its auto-run off for the read (or delete the column). It spends credits on every visit row and the scorer does not read it; company data comes from Audiences at 0 credits, and we enrich only rep/cohort-lane companies later.

## Part 2: Carter or Sierra, in Google Tag Manager (about 5 minutes)

Send them this (snippet included, received from Clay Aug 25 2026):

> The Clay visitor-tracking snippet for intradiem.com is below. It is a standard async script; company-level only, no personal data, same category as the LinkedIn Insight tag. Install in the GTM container `GTM-WSDB2RL`:
> 1. Tags, then `New`.
> 2. Name it `Clay Visitor Tracking`.
> 3. Tag Configuration, choose `Custom HTML`, paste the snippet.
> 4. Triggering, choose `All Pages`.
> 5. Save, then `Submit` and publish the container version.
>
> Snippet (paste exactly, nothing else in the tag):
> ```
> <script src="https://static.claydar.com/init.v1.js?id=cXnI1ezWwu"></script>
> ```
> After publishing, load intradiem.com in a normal browser window, open DevTools, Network tab, filter `claydar`; a request should appear on page load. Tell me when it is published and I will confirm sessions are landing in Clay.

Questions to ask Sierra in the same note: is a 6sense tag in that container (it is not visible in the page HTML), and do 6sense buying-stage fields sit on the Salesforce Account object?

## Part 3: after sessions land (mine)

- Paste the table id into the config; run `python3 automation/heat_list_scorer.py --dry-run` and read the log with you once.
- The 14-day read sets the monthly credit cap; the ledger row is written then.
- Nothing flips to live (stamps, Lane A alerts) without your explicit go.

## Ordering traps

- Part 1 step 3 before step 5: the table needs the connection to exist.
- Do not publish the GTM tag before the connection exists in Clay; the script would load with no destination.
- LinkedIn's Insight tag and the Clay tag both fire on All Pages; that is normal and does not double-count anything.
