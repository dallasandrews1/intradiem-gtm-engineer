# UI sheet: create the "Jack UK Airlines" workbook (Clay UI, Dallas's hands)

Aug 1 2026. Table/workbook creation isn't available through the CLI, MCP or API on the current setup, so this one step set is yours. Everything else in this motion (sourcing, variables, critic, Lemlist campaign) is already done agent-side; this sheet just lands the data in a standing workbook so waves can pull from it.

**Do nothing in Lemlist from this sheet.** The DNC gate is still open and the campaign stays Draft with zero leads.

## Order matters

1. **Wait for nothing.** The import CSV is final: `motions/uk_airlines/UK_Airlines_Committee_Contacts_v1.csv` (76 rows, 17 columns, variables and critic verdicts already in it).
2. In Clay, create a new workbook named **Jack UK Airlines**. (Workspaces sidebar, the same "+ Create new" flow you used for the Back Office Motion workbook; I'm not fabricating the exact button label, flag if it's moved.)
3. Inside it, create a table by **importing the CSV** above. Name the table `UK_Airlines_Committee_v1`.
4. Verify the import kept all 17 columns, especially: `wave_number`, `wave_status`, `status`, `opener_line`, `contract_line`, `vm_hook`, `voice_script`, `critic_verdict`, `prompt_version`. They should all land as plain text columns. No enrichment columns, no auto-run anything.
5. That's it. Do NOT add email-waterfall or phone columns yet; that's a per-wave credit decision that only fires after Jack's DNC list lands and the wave is cleared.

## What is deliberately NOT in this build

- No email or phone enrichment (zero credits spent; the sourcing was Clay native search, which bills nothing).
- No Clay-side MessageGen/critic AI columns. The four variables were generated by Claude through the copy gates and critic-reviewed; Clay-side generation stays a paid-rollout decision on receipts, per the Jul 31 architecture call.
- No Lemlist lead load, no sync, no campaign start.

## Traps

- The two Jet2 "Louise Smith" rows: one is wave-1 with variables, one is flagged `DUPLICATE-SUSPECT`. Verify which profile is current before any load; don't delete the flagged row until then.
- TUI Airways is absent (Clay's dataset wouldn't resolve it, three identifier passes tried). The universe doc logs it; don't treat the table as TUI-complete.
- `status` on every row reads `NOT LOADED - pending DNC gate`. That column is the gate; nothing changes it except the DNC check on real rows.
