---
name: brain-live-loop-plan-sep11
description: "Sep 11 2026 plan from the TAM-swap thread: make the brain a real loop in four moves (accounts from Audiences not CSV, triggers with an unreviewed-to-cited state flipped by Slack approve/deny, nightly --deploy not stage, weekly scheduled research refresh); that thread owns the build including the sync_publish deploy flag"
metadata:
  type: project
---

Dallas relayed the loop thread's assessment Sep 11 2026 evening: today's TAM swap replaced one static file with a better static file (credibility fixed, nothing seed served) but is not the loop. Live already: war room, heat-list scorer, Audiences SF sync, lemlist relay. Frozen by design: engine inputs (hand-assembled CSV, triggers merge only by hand since the Sep 5 seed incident, platform reads a one-off Sep 5 pull, nightly stages only).

Four-part plan, none a large build, generator and brain already carry the provenance contract:
1. Accounts from Audiences at build time (SF prospect status, industry, owner, headcount, platform reads, 0 credits; industry map exists).
2. Triggers carry a state: staged signals enter as "unreviewed", brain labels them so, Slack approve/deny flips to cited (same mechanic as the heat loop). Human checkpoint stays.
3. Nightly deploy, not stage: one flag in sync_publish step 4b.
4. The seven research agents become a weekly job writing to the same store.

**Why:** two threads found the same publish-leg gap independently ([[brain-snapshot-publish-leg-manual-sep11]]); the loop thread owns all four items so sync_publish is edited once.
**How to apply:** the Slack-app thread does not touch sync_publish. Slack approve/deny for item 2 must ride the existing rundown thread listener, never new scopes on the Intradiem GTM Brain app (single scope mcp:connect, described that way in the pending admin request, [[slack-gtm-brain-app-created-sep11]]).
