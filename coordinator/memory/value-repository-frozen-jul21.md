---
name: value-repository-frozen-jul21
description: Value Repository frozen as a prospect-facing source Jul 21 2026; only Humana + public CMS math + DWO positioning are cleared to ship until a value-only allowlist is rebuilt from activity counts
metadata:
  type: project
---

Per Dallas's directive after the [[matt-graves-intro-call-jul21]], the Intradiem Value Repository (`04-value-repository/Intradiem_Value_Repository.md`) is **frozen as a prospect-facing source**. A FROZEN banner was added at the top of the file Jul 21 2026.

**CLEARED-TO-SHIP allowlist (the entire current surface):** Humana webinar claims + public CMS Star Ratings math (labeled estimate) + the DWO positioning statement. Nothing else.

Everything else (per-account CTO, Zuar/Greenlight savings, whitespace, maturity) stays **internal targeting only** and does NOT promote to prospect copy. The verified-claims GATE stays on — the allowed list shrinks, the discipline does not relax. New value list will be rebuilt from objective activity counts (actions/agent/day, coaching sessions, adherence/handle-time alerts) once Josh's denominators are fixed.

**Why:** Matt Graves confirmed Zuar savings are an internal averaged model customers will dispute; naming customers is a contracts/marketing gate. Humana is the sole cleared named exception.

Skills that gate against this file (intradiem-verified-metrics, intradiem-copy-sharpener, gtm-copy-reviewer, MessageGen `product_angle`) will now only pass Humana/public claims. Not yet updated the skill bodies — the banner does the gating for now; revisit if a skill needs its text tightened.
