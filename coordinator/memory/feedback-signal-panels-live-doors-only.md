---
name: feedback-signal-panels-live-doors-only
description: A signal panel on a rep page shows only the rows that call for action, one clause each; non-actionable rows fold into a single collapsed line
metadata:
  type: feedback
---

Sep 23 2026, Keegan door panel. Dallas: "is this the best way to illustrate this so it flows naturally and is not hard to follow? Would you use this as-is?" First version listed every customer spell (10 rows, 6 of them "left before Intradiem arrived") with paragraph-length use text, and cards repeated the table.

**Why:** rows that mean "do nothing" dilute the ones that mean "call"; a rep scans, never reads.
**How to apply:** panel = actionable rows only, columns Who | Where, when | Open with (one clause) | Ask first; everything else in one `<details>` line with a count. Cards carry one line per live signal and one grey line for the rest. Ship the version I would use myself, then ask. See [[feedback-no-fluff-scannable-deliverables]], [[keegan-door-context-sep23]].
