---
name: wfm-adjacency-and-belfield-continuation
description: "WFM-Adjacency L1 signal/scoring layer fixed and saved live Jul 17 (Motion, Fit Score, top_signal, Intent Score all rewritten off Cost-Mandate logic, four sig_* booleans dropped, Claygent prompt rewritten for tech-stack/leader-change signals); 28-company new-logo import list prepped, credit-gated sample run still pending Dallas's go; Belfield strike-room backlog has 10 accounts remaining after Blue Shield CA + Aflac set the quality bar"
metadata:
  node_type: memory
  type: project
  originSessionId: catchup-jul17-2026
---

`WFM_Adjacency_Clone_Pack_v1.md` is the pre-filled clone kit for the next motion off the New-Logo golden scaffold (see [[clay-motion-factory-scaffold-system]]). The blocker it documented — `fn_persona_key` had no `wfm` branch, every WFM job title routed to `out_of_icp` — was fixed and verified live Jul 17 (see [[clay-functions-build-status]]). Clone execution is live in the `WFM-Adjacency Motion` workbook: L1 table ID `t_0tic88ceqvEVhK2B8gn`, L3 table ID `t_0tic8arWbZp8bSx87Ad`. Working note: `clay tables list` can't enumerate this workbook's tables (Enterprise-only endpoint), but reading a table by ID (schema + rows) works fine once you have it. The `clay` CLI's `tables` command is read-only (list/get/columns/rows/query) — no row-write path exists in any available tool, so seeding new company rows into a Clay table is a Clay-UI-only action, same as the earlier finding that scaffold-duplication is UI-only.

**Step 1c completed live Jul 17** (a separate session from the one that did the clone/repoint): L1's signal/scoring layer was still Cost-Mandate's cost-pressure model when checked (Intent Score summing `sig_rif`/`sig_efficiency_mandate_public`/`sig_margin_pressure`/`sig_hiring_freeze`; `Motion` hardcoded to `"cost_mandate"` for every row) — none of it had been reauthored for WFM. Fixed and saved: `Motion` → literal `"wfm_adjacency"`; `Fit Score` → payer/health-plan/health-BPO industry fence + size + US-geo, single-expression ternary (Clay's formula columns here only accept one expression, not multi-statement JS with `const`/`return` — every working formula in this table is a nested ternary chain, confirmed by inspecting the live ones before writing replacements); `top_signal` and `Intent Score` → rewritten around `techstack_change`/`wfm_leader_change`/`hiring_surge` labels parsed from the Claygent output; the four `sig_*` cost-pressure booleans deleted (nothing references them now). `Job Openings (WFM/RTA)` needed no change — its existing keyword search (real-time analyst, workforce management, WFM, capacity planning, intraday) already fits the WFM hiring-surge signal, built for Cost-Mandate by coincidence. `Cost Signal Research (AI)` prompt rewritten to detect tech-stack deployment/expansion (Verint/NICE/Calabrio/Genesys/Amazon Connect) or a new WFM/Care-Ops leader (≤90 days in role) within 180 days, output format `yes | <label> | <evidence>`. L3 needed no changes — `Source Motion` already literal `"wfm_adjacency"`, `persona_key` already calling the fixed `fn_persona_key`, the MessageGen Email 1 (WFM-Adjacency) system prompt already correct and high-quality.

**Sourcing (Jul 17):** deduped the 98-contract Star Ratings Tier A+B CSV to 32 unique parent orgs. 4 are existing Intradiem customers per the Star Ratings customer-exclusion work (Humana, UnitedHealth Group, CVS Health, Health Care Service Corporation) and must be excluded — `New Logo Eligible` should catch these automatically via `Install Base Lookup (BO Universe)` once loaded, but spot-check after import rather than trust blind. 28 new-logo-eligible parents written to `Clay Builds and Strategy/WFM_Adjacency_L1_Import_Wave1.csv` with domains filled in only where confident (Clay's own search guidance: never fabricate a domain from a company name), several left blank/flagged for manual Find-Companies resolve. Separately ran Clay's native people-search (`clay search filters-mode`, not the `claude.ai Clay` connector — that's a different product, "clay-for-reps," per the Terracotta plugin's own instructions) for the leadership-change half of the signal: found 3 candidates, but 2 (Alyssa Fleming, Cameron Bayly) turned out to be at CVS Health/Aetna — an excluded existing customer — leaving only Sona Khare (Healthfirst, Director Org Design & Strategic Workforce Planning, started 2026-02-01) as a live lead, and Healthfirst isn't even in the 32-parent Star Ratings universe. Lesson: cross-check any hand-sourced lead against the customer-exclusion list before treating it as real, not after.

**Still open, needs Dallas:** (1) run the 28 rows through Clay's Find Companies step to populate L1 (UI-only); (2) the credit-gate sample run (Part A3 of the Week1 Execution Pack — 10-row sample, log actual cost, extrapolate, ledger row before firing the full batch) hasn't happened, and depends on (3) the still-unresolved credit-allocation question from the ledger merge — WFM-Adjacency's proposed 3,500/month doesn't fit what's left of Reserve once Cost-Mandate is counted, see [[clay-credit-ledger-state-jul16]].

**Correction, later same night (Jul 17-18): the sourcing/company-import step is NOT solved, despite a separate parallel Cowork session claiming the golden-scaffold system is "PROVEN end-to-end."** Verified directly, not assumed: `clay search filters-mode create --source-type companies --filters {"include_company_identifiers": [...]}` (bypassing Sculptor and the UI entirely) against the 28 target domains returns duplicate sub-entities for some domains (8 of 20 first-page results were all `massgeneralbrigham.org` divisions — Salem Hospital, MGB International, MGB AI, MGB Data Science Office, MGB Ventures, MGB Healthcare at Home x2) and **zero results for 14 of the 28 domains, including centene.com and elevancehealth.com** — two of the largest targets. This is a genuine data/API coverage gap, not a Sculptor prompting problem — confirmed by testing the raw search API directly. Bulk "Find Companies by domain" (any interface: Sculptor chat, manual UI filters, or CLI) is not a reliable way to seed a table with a specific known list of companies; it's built for open-ended discovery, not exact-list import.

**What IS solid:** `persona_key` fixed, MessageGen Email 1 (WFM-Adjacency) prompt strong and ready, L3's work-email waterfall intact, and L1's `Motion`/`Fit Score`/`top_signal`/`Intent Score` formulas (rewritten off Cost-Mandate leftovers) save clean now after several delete-and-recreate cycles cleared stuck column state (Clay's formula columns take a single expression only — no `const`/`return`/multi-statement JS — and columns can get into a broken save-state that editing in place won't clear; delete-and-recreate with the identical name fixed it every time, and downstream formulas referencing the field by display name kept working). **The credit-gated 10-row sample of `WFM Signal Research (AI)` + `Job Openings (WFM/RTA)` has never actually run** — nothing on the signal-research side is proven against real data yet, only believed-correct.

**Recommended next step (not started):** don't build a parallel SRC/UNIV sourcing layer (per the Autonomous Engine Plan) while L1's own sourcing is unfinished — that recreates the exact two-competing-builds problem from earlier the same day, just shifted to the sourcing layer specifically. Instead: test whether Clay's `+ Add` button lets you manually add one blank row to L1, seed it with a known-good domain, and run a per-row `Enrich Company` action (function:t_0thx4ohppDHidDJYaYf) against it — that's the pattern that actually worked cleanly earlier (the `Company Domain` routine, function:t_0thx4oeCWHZgydZRRVv, resolved all 28 real domains correctly via exact per-company lookup, zero noise). If manual one-at-a-time add works, repeating it 28 times is tedious but reliable, unlike another bulk search attempt.

The 28 target companies with Clay-verified real domains (not guessed) live in `Clay Builds and Strategy/WFM_Adjacency_L1_Import_Wave1.csv` — trustworthy regardless of the import-mechanism problem above. 4 known customers (Humana, UnitedHealth Group, CVS Health, Health Care Service Corporation) already excluded from that list.

**LATE Jul 17 — L1 is corrupted by ID rot; DO NOT patch it further, rebuild clean. Runbook written.** Two things resolved and two lessons, then the table broke:
- SOLVED — import mechanism: bulk "Find companies" search is unreliable for an exact list (dupes on some domains, misses on others incl. Centene/Elevance). The fix is to seed by **CSV import of pre-enriched rows**, not search. All 28 companies were fully resolved via Clay's `Company Domain` (function:t_0thx4oeCWHZgydZRRVv) + `Enrich Company` (function:t_0thx4ohppDHidDJYaYf) CLI routines — name, domain, LinkedIn URL, industry, size, country — and written to `Clay Builds and Strategy/WFM_Adjacency_L1_Seed_Enriched_28.csv` (import-ready). Searching by exact **LinkedIn company URLs** (not domains) also returns a clean 1-per-company set if search is ever needed. ~42 credits total spent Jul 17 on this resolution work (pool 71,916 → ~71,860), logged in ledger.
- LESSON — Clay binds formula refs by internal column ID, not display name. Delete-and-recreate a column and every downstream formula keeps pointing at the OLD dead ID even if the new column has the same name. Repeated delete-recreate cycles this session orphaned: identity cols → deleted `Find companies` source; `Grade` + `intent_status` → deleted old `Intent Score` ID; `WFM Signal Research` → deleted `Domain`. The table `t_0tic88ceqvEVhK2B8gn` now has rot in 4+ places. **Rebuild rule: apply formulas by EDITING IN PLACE on fresh columns, never delete-recreate a column others depend on.**
- LESSON — Clay's plain-English "Formula generator" box wraps expressions in quotes (turns them into string literals). Paste real formulas into the regular formula field only; the generator box is for literals like Motion.
- Full clean-rebuild procedure (seed method, all formulas in dependency order, field-name/ID warnings, credit-gate sample step): `Clay Builds and Strategy/WFM_Adjacency_L1_Rebuild_Runbook.md`. Decision made end-of-session: don't keep patching a rotted table at the tail of a long day; rebuild in one calm pass when fresh. Nothing lost — L1 is at 0 rows and every formula + the seed data is captured.

**REBUILT AND LIVE (2026-07-18, same long session, continued).** The rotted L1 was abandoned. New clean account table built by CSV import: `WFM_Adjacency_L1_Seed_Enriched_28` (table ID **`t_0tict25TSXgTgdJgtZZ`**, in the WFM-Adjacency Motion workbook) — 28 verified rows, 7 plain identity columns (company_name, domain, linkedin_url, primary_industry, size, country, customer_flag), zero source dependency, zero rot. Full scoring layer applied and WORKING: `Fit Score`, `New Logo Eligible`, `Intent Score`, `top_signal`, `intent_status`, `Grade`, `Motion`, plus two enrichment action columns `WFM Signal Research (AI)` (Claygent clay-argon, the WFM-wedge prompt) and `Job Openings (WFM/RTA)`. Install Base Lookup + customer_match_key were dropped for this batch (the 4 known customers are pre-excluded from the seed; re-add the lookup for the future automated version where rows arrive un-vetted).

**Credit-gate sample RAN and passed:** 10 accounts (Centene, Elevance, Molina, Clover, Cambia, VNS Health, Imperial, Medica, Zing, Clever Care), **31 credits = 3.1/account**, far under the pre-estimate. Full 28 ≈ 87 total, remaining 18 ≈ 56. Scoring computes live; sort by Intent Score to rank. Logged in ledger.

**THE decisive Clay-formula lesson (this cost hours): Clay's "Formula generator" box — the one with the purple Generate button and "What formula would you like to create?" — WRAPS whatever you give it in quotes and stores it as a string literal, so the column outputs the raw formula text instead of evaluating. ALWAYS enter real formulas in the plain Formula column type's raw field ("Reference data from a column, use a formula, or enter static text"). The tell that a formula is live: syntax coloring (variables/strings different colors), not one solid color. Every "won't save / Invalid field / returns literal text / null Number" symptom this session traced back to this one box, plus Clay binding refs by column ID (so delete-recreate orphans dependents — rebuild leaf-up or in dependency order and bind refs via autocomplete).**

**COMPLETE (2026-07-18): full 28-account signal pass done + L3 repointed.** All 28 accounts scored (~85.5 credits total signal research, ~3/account, on estimate). L3 (`t_0tic8arWbZp8bSx87Ad`) repointed live: Universe Lookup Table ID → `t_0tict25TSXgTgdJgtZZ`, Target Column → `domain`; Draft Audit signal_evidence key → `["WFM Signal Research (AI)"]`. L3 was verified fully intact (50 columns, Find People source alive, waterfall + MessageGen + critics all good) — only those 2 dead-L1 refs needed fixing, no rebuild. Note: Clay UI makes you pick the lookup table from a dropdown by name (`WFM_Adjacency_L1_Seed_Enriched_28`), can't paste the ID.

**OUTCOME — signal is sparse: only Zing Health fired** (wfm_leader_change, Intent 35, Grade C); the other 27 returned `none` (Intent 0). Engine works clean end-to-end; the thin harvest is either genuinely narrow signal or Claygent under-calling (directional, treat like the Stars leadership signal — a Grade-A/B `none` isn't proof of no signal). **Tuning levers for a wider next pass:** widen window 180→365d, loosen Claygent validation strictness, lower the hiring_surge threshold from ≥3 (likely too high for payer WFM roles — try ≥1-2). Open check: confirm the Job Openings (WFM/RTA) column returned real Job Counts in-grid (my read tools can't extract nested action outputs; the query engine + `clay tables rows` CLI are both gated on this workspace).

**Buying committee SOURCED (2026-07-18):** 14 contacts across 8 accounts (Centene flagship + Elevance/Molina/Blue Shield CA/Point32/CareFirst/VNS + Zing) via `clay search filters-mode` people search — champions (VP/Dir WFM, Contact-Center Ops) + economic buyers (VP CX/Member Services). Saved: `Clay Builds and Strategy/WFM_Adjacency_Wave1_Committee.md`. Centene = strongest target (Fit 100 + 10 open WFM roles = live hiring_surge + VP Christopher Salvatore + VP Jesse Korella reachable).

**KNOWN ISSUE blocking the message payoff — L3 contact seeding (2026-07-18):** tried seeding L3 via Find People search → "Import 195 rows". 192 landed but ALL identity columns resolve null — L3's identity is FORMULAS off a person object (`{{f_people_search}}.matched_experience.company_name` etc.) and an import gives shallow rows without that object, so persona_key/waterfall/MessageGen have nothing to read. Same family as L1's seed-vs-formula-identity snag. Fix (fresh session, needs live inspection): either (A) run L3's NATIVE Find People source so f_people_search populates full objects, or (B) L1-style — make identity the plain imported columns and repoint the pipeline reads. 192 hollow rows are harmless (0 cost, auto-run off). Full detail + fix options in `WFM_Adjacency_CONTINUE_HERE.md` STEP 3.5.

**Also pending:** the Job Count scoring fix (add `?.Jobs?.length` fallback to Intent Score + top_signal so Centene's 10-job hiring_surge registers — Centene showed Intent 0 despite 10 WFM openings). Formulas given last session.

**Next (the payoff phase, fresh session):** FIRST fix L3 seeding (STEP 3.5), then source buying-committee contacts into L3 at the top-fit accounts (Grade A/B) — 2-3 per account (champion: VP/Dir WFM/Workforce Optimization/Contact-Center Ops/Resource Planning; economic buyer: VP/SVP Customer Care/Member Services/Care Ops) — then the already-built L3 pipeline (persona_key → email waterfall → MessageGen → Draft Audit → Voice Audit → send gates) runs per contact. Two MessageGen variants already drafted + gated PASS. Nothing sends without explicit go; sender still unconfirmed (default Nathan Belfield). Handoff doc: `Clay Builds and Strategy/WFM_Adjacency_CONTINUE_HERE.md`. Because the signal harvest is thin, consider running the tuning levers BEFORE sourcing contacts, so the outreach targets accounts with a live trigger rather than fit alone.

**RESOLVED (2026-07-18, later): MessageGen "ERROR wrong motion" cleared + run surface decided.** The plain-text isolation test passed (hardcoded Prompt with `source_motion: wfm_adjacency`, no chips, generated real emails). Cause confirmed: unbound `{{}}` chips in the pasted Stars-format concatenation (paste doesn't bind by column ID; also used Stars column names absent in WFM). **Decision: run the L3 MessageGen via the existing WORKFLOW, not the table column.** A complete 24-node workflow `WFM-Adjacency Send-Readiness (Alpha)` (`wf_0tic9xaFeq2rKvMnKuD`) already exists with a correctly-bound MessageGen agent node (all 17 tokens incl. source_motion wired as workflow variables) + full gate chain (eligibility, persona, email-verify, H1 source gate, tokens-ready, malformed guard, figure critic, voice audit, send-ready). Validated clean. RUN IT ON REAL TABLE ROWS in the UI (the 6 curated committee contacts: Jessica Sisneros, Melissa Zam, Noemi G., Carlos Doroteo, Donisha Jones @ Elevance; Linda Reid @ VNS). Do NOT run via CLI `clay workflows runs test --input` for real rows — gate function `fn_eligible` (`!install_base && customer_flag != true`) depends on real null-vs-empty column semantics that synthetic JSON can't reproduce (empty string fails the node's required-input check; the eligible formula needs falsy). LESSON (now a CLAUDE.md rule): before any manual multi-step Clay work, run `clay workflows list` and prefer an existing workflow/function over hand-building. A weekly cloud "Automation Strategist" routine (`trig_0176q8Xh2HohHkdCdCG7hAMo`, Mon 7am CT) was created to surface automation opportunities from Mem0 + DM them on Slack.

`Opus_Continuation_Prompt_Belfield_StrikeRooms.md` is the copy-paste handoff for continuing Nathan Belfield's 12-account strike-room backlog: Blue Shield of CA and Aflac are done (the quality bar, see [[cost-mandate-strike-room-accounts-jul16-17]]); GM Financial, T-Mobile, CapOne, Fidelity, Morgan Stanley, Walmart, National Grid, Avangrid, Lumen, IKEA remain. Specifies the exact build/enrichment/ZeroBounce/PDF pipeline and hard rules: Clay-only enrichment (never Apollo — Apollo MCP is free-tier and blocked, per [[contacts-list-finalized-jul13]]), nothing sends without Dallas's review.
