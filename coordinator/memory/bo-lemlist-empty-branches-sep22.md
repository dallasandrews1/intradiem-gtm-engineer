---
name: bo-lemlist-empty-branches-sep22
description: "Sep 22 2026: BO lemlist campaigns rebuilt to the branch skeleton; Payer/Ins/BPO moved to v2 campaigns because lemlist locks sequences once leads enter; all leads moved, nothing launched"
metadata:
  node_type: memory
  type: project
  originSessionId: b37fd0f6-84a0-4d6b-86e3-38bb0786240b
  modified: 2026-09-22T23:38:00.104Z
---

Sep 22 2026. The four BO Expansion campaigns had empty accepted/fallback branches (steps stranded on the root after the condition); Net-New had no branching. Rebuilt from BO_Copy_v2_Sep9.md (Version C) on the Blitz / Stars Finance skeleton. Everything lives in `motions/back_office_expansion/bo_rebuild_sep22/` (spec.py = copy + tree, build.py = REST builder, dry run default, branches.json = every seq id, BO_Rebuild_Copy_Sep22.md = review doc). gtm-copy-reviewer fixes applied.

State: FS (cam_x8ehMHnWSjBr2CLQe) and Net-New (cam_DNErdZPANvC2sqRCK) rebuilt in place, paused. New drafts with root split on lead variable `bo_e1_sent` (TRUE = got Sep 2 Email 1, follow-on path opening "re: <old subject>"): Payer v2 cam_tysjbaAP4vrcCATCJ, Insurance v2 cam_wLsY4xhaBH3tMdgkc (33 leads, tagged), BPO v2 cam_oYTypEKTjbs6TDZrt (24 leads, tagged, unsubscribed Drex Fitzwater removed). Payer copied Sep 22 on Dallas's paste-back: 66 leads in v2, 31 tagged, bounced Conviva lead removed, Four drifted emails (Altig, Villanueva, Rudolph, Lorenzon) were Salesforce-sync overwrites; Clay Enrich Person Sep 22 confirmed all at original employers, Clay emails restored in lemlist (Rudolph is NOT at Humana; reverted to Molina, follow-on path). Lorenzon's current title is now MetLife risk (US CRO line), a Nate review call. UHC-to-healthcare-services proof swap verified by preview. Old Payer/Ins/BPO campaigns archived Sep 22 on Dallas's go. Leads whose email drifted must be matched by name, then restored from Clay. National Grid opener_line replaced (no wound opener).

lemlist mechanics learned: once any lead enters a campaign, NO sequence in it accepts new steps (pausing doesn't help), so rebuild in a new campaign; never-launched campaigns allow step DELETE and adds. REST calls need a User-Agent header or Cloudflare returns 403/1010. Manual linkedinVoiceNote steps ARE accepted inside a branch (only AI voice is refused). create_campaign_with_sequence reports "running" but the campaign is a draft. Lead variables: POST adds only when the key is new to the campaign; after that use PATCH.

Pre-launch audit Sep 22 fixed: Clay Enrich Person on all 207 FS/Net-New leads (~1,650 credits): nobody left their company; 6 wrong/odd emails restored to Clay values (Heavener gs.com, Craig regions.com, Cattani centene.com, Arif Mohammed synchrony.com, Rumignani chase.com, Joe King centene.com); Cecilia Sun removed (at JPMC, no Clay work email); 164 US mobiles written (phone coverage 26 to ~190 of 208). Reply settings on all 5 now match Blitz Hartford (stop on reply + task, AI pause-all on scored replies). BLOCKER: API-made manual voice-note steps fail readiness ('Your step must have a specific sender'); 8 need Nathan picked as sender in the UI, sheet motions/shared/polar_sheets/Lemlist_Sheet_BO_VoiceNote_Sender_Sep22.md, polar task lemlist-bo-voicenote-sender.

Launch plan (decided Sep 22): hold until Nathan signs off the copy (canvas), his dialer test passes, and the lemlist-lead-integrity audit on real rows is CLEAR; then start FS + Net-New and launch the three v2 drafts.

Nathan's number: he had zero call tasks ever and zero dialer calls. Caller numbers are chosen in the dialer by the logged-in user, not on the call step.

Related: [[lemlist-nathan-invite-steps-manual-sep22]], [[nathan-lemlist-phone-backfill-sep22]], [[lemlist-salesforce-sync-overwrites-lead-fixes]]

**Pressure pass Sep 22 2026 (late): launch now blocked on copy and list, not just the voice-note senders.** Two adversarial reviews, re-verified: the follow-on path (87 leads) never says what Intradiem does (Sep 2 Email 1 was Version A); Optum is named on calls and voicemails but is cleared for email and LinkedIn only; about 30 FS IT/product-owner seats and about 30 WFM seats across lanes are off-persona; Insurance HOLD for a list re-cut; peaks wrong (AEP is not a claims peak, FS year-end close is controllership). Three claims-review findings were false (Net-New blank opener, UHG in Net-New, National Grid wound opener live) — live lemlist already has the fixed National Grid line; only netnew_package_config.json line 442 is stale. Plan + draft lines: motions/back_office_expansion/bo_rebuild_sep22/Pressure_Pass_Sep22.md. Order: pull seats, Insurance re-cut, confirm no Sep 2 Email 2 sent, text edits, Lane 2 sources, integrity audit, senders, Nate's read.

**Seat pull STAGED Sep 22 2026.** Live export: 331 leads, none launched (all status "review", so the UI action is Skip, reversible via Unskip; Pause only exists for launched leads). 122 to skip: Payer 20, FS 36, Ins 20, BPO 12, NN 34 (12 NN WFM seats come back once the WFM copy variant is live; Holly Allard and a no-title Foundever lead need Clay first). Files in bo_rebuild_sep22/seat_pull/ (CSV, snapshot, verify_skips.py baseline ran clean at 122 not-skipped / 0 changed); sheet motions/shared/polar_sheets/Lemlist_Sheet_BO_Seat_Skip_Sep22.md, polar task lemlist-bo-seat-skip. lemlist REST export (GET /campaigns/{id}/export/leads?state=all&format=csv with build.py auth) reads a whole campaign in one call, far cheaper than the MCP search at limit 10.

**Text fixes APPLIED Sep 23 2026 (FS + Net-New only, campaigns stay paused).** 41 steps edited in place, then 12 more on the gtm-copy-reviewer pass (PASS). Peaks: FS "the January peak", NN Centene "the January 1 wave", National Grid winter season, rest "the January peak"; NN Email 4 now "works inside the staffing plan and WFM you've already set, so nothing about the schedule changes to start"; finding-promise asks reworded ("see how it runs on a floor like yours"), including the call-script THE ASK line; breakup wound line gone; Optum off every voice step ("a health insurer's back office"); FS 18.4 repeat swapped for the 14 idle hours line. Re-read of all 68 steps matched. Tooling + before/after snapshots: bo_rebuild_sep22/textfix_sep23/. spec.py and BO_Rebuild_Copy_Sep22.md are now STALE against live for FS and NN; read live before any rebuild. Payer/Ins/BPO v2 text fixes NOT applied.
Seat pull happened by REMOVAL, not skip: 108 leads removed Sep 23 across the five campaigns (backup seat_pull/removed_leads_backup_sep23.json, re-importable). Live Sep 23: FS 42 leads, NN 108; the 12 NN "workforce planning" seats (PAUSE_UNTIL_COPY) are still in Net-New and would get the wrong angle.
**Sep 23 2026 incident (reported by session c7ccf6):** Payer v2, Insurance v2 and BPO v2 were launched at about 10:08 CDT with unfixed copy, then paused 10:50 CDT; c7ccf6 owns the BO launch and is applying corrected step plans. The FS/NN text-fix session did not launch anything and stopped writing to all five on c7ccf6's request. c7ccf6 confirmed it rebased its FS and NN plans on the live post-fix text, so the Sep 23 fixes carry forward.

**Sep 23 2026, launch day (session c7ccf6, Dallas's "finish end to end" paste).** Seats: 108 PAUSE leads deleted over the connector before any send (backup seat_pull/removed_leads_backup_sep23.json); Holly Allard and Mike Corrigan cleared by Clay and kept; the 12 Net-New WFM seats stay on the WFM Liquid variant. Insurance re-cut from 13 to 34 through Clay CLI search plus Work Email, Enrich Person and Contact Details (Assurant left out, Frank's live sponsor). Copy: 159 step fields plus 18 round-2 fields applied over REST PATCH /sequences/{seq}/steps/{stp} (body needs "type"), 83 Net-New opener lines by PATCH /campaigns/{cid}/leads/{email}; every field read back exact; three gtm-copy-reviewer passes (pass 1's three findings were false, the customer-lane opt-out line is required; pass 2 caught Email 3 with no direct ask). Lane 2 file bo_rebuild_sep22/Lane2_Recheck_Sep23.md. Canvas for Nate: https://intradiem.slack.com/docs/T02FUS29E/F0C44LAJ9HP (rebuilt with paragraph spacing, not shared; F0C426DH9AQ is the discarded first cut). Readiness ready on all five (voice-note senders were set in the UI this morning).

**Two things that happened around me.** (1) Someone launched Payer v2, Insurance v2 and BPO v2 from the lemlist UI at 10:08 CDT with the unfixed copy; 25 step 0 emails went, 7 bounced (Clay confirms all 7 addresses, so mailbox rejections, they stay bounced); I paused the three at 10:50 CDT. (2) Session 0e rewrote FS and Net-New step text in two passes (textfix_sep23/); I rebased my plans on its live text instead of reverting it. Lessons: read every campaign's status and a sequence diff right before any apply, never trust a morning capture; four interactive sessions can be live on this Mac at once, ListAgents plus SendMessage settles who owns what; never use re.sub with a raw-string replacement containing \' (it wrote literal backslashes into 12 call scripts). The Salesforce sync reverted Heavener and Craig again (restored from Clay a second time); Salesforce still needs the Clay values pushed.

**LIVE Sep 23 2026, 12:05 CDT.** All five running: Payer v2 47, FS 42, Insurance v2 34, BPO v2 13, Net-New 108. FS and Net-New needed launch_campaign after start (start on a never-sent campaign yields "draft"); the 21 new Insurance leads needed their own launch_campaign. Auto-review is off everywhere. Log automation/logs/bo-lemlist-rebuild-2026-09-22.md (evt bo-rebuild-2026-09-23#launched).
lemlist schedule lesson: a campaign's sending schedule is its own object; POST associate from another campaign fails, PATCH /schedules/{skd_id} with timezone, start and end edits it in place. Payer v2 and Insurance v2 shipped on Europe/Paris hours (copied from a template) and were fixed to New York on Sep 23.
