---
name: mission-control-dashboard
description: "Dallas's private mission-control health dashboard (live artifact + HTML file) for the Intradiem role"
metadata: 
  node_type: memory
  type: project
  originSessionId: cb230ac8-4bee-49da-abe5-b183266240fb
---

Dallas's private "behind the curtain" command center, built Jun 23 2026. Live as artifact id `dallas-mission-control`; source HTML at `/Users/dallasandrews/Claude/Projects/Intradiem GTM Engineer/mission-control.html`.

Design: 30-second morning glance (status light + active warnings + today's top 3, all computed by JS rules from STATE) over 7 collapsible panels — Q3 mandate progress (provisional bars), Week-1 instrumentation/attribution keystone (checklist), tool/system health, relationship health (Naveen green, Nate Belfield + Scott Kemme amber = manage), Promise Ledger (his #1 over-promise risk), Proof/Receipts log, and honest Trend (stays empty until ≥2 real data points — deliberately no fake lines).

Self-contained today (no live connectors — he doesn't start until Jul 6, nothing real to pull). Each panel has a "wire:" hint for the connector it'll bind to later (Gmail, Apollo, Clay, his signals/TAM MCPs). Interactive checkboxes + ledgers persist in localStorage; the SEEDED STATE (mandate currents, tool statuses, relationship health, flags) is updated by Claude via chat. To update: edit the STATE object in the HTML, then re-run create_artifact with same id, OR update_artifact.

When Dallas reports progress (meetings booked, attribution locked, a promise made, a win), update the relevant STATE field so the glance + flags recompute. See [[intradiem-q3-mandates]], [[intradiem-interview-status]].

HARDENED Jun 23 2026 after a strategic-premortem + assumption audit (Dallas asked for both). Premortem's core finding: the dashboard is a "Blind Oracle" — it can only mirror what Dallas inputs, so it structurally can't catch what he's not seeing, and risks lulling him. Fixes shipped: (1) **Outside Read panel** — the only sensor pointed outward: logs verbatim quotes from other people (source, quote, optional read, "didn't want to hear it" checkbox) + a `naveenWorking` field for Naveen's own words on what "working" looks like at 90 days (his REAL scoreboard). (2) Loud staleness alarm: no outside read in 7 days → RED flag; none ever → flag. (3) Flattery detector: 2+ reads in 30d and none disconfirming → amber. (4) Grooming guardrail: counts tool edits/week (localStorage `edits`) vs real moves (proof+signals logged that week); edits≥6 and >2× moves → amber "groom less, move more." (5) Q3 mandate bars reframed as Naveen's "rough sketch," neutral color until `targetsConfirmed` toggle checked — so a low bar doesn't manufacture panic/volume-trap. (6) TODAY is now live `new Date()` (was frozen at 2026-06-23) so alarms fire. The #1 seeded action: ask Naveen what "working" looks like and log his exact words. Verified via jsdom: 8 panels render, 0 runtime errors.

EXPANDED Jul 2 2026 (gap analysis vs real Q3 mandates): now 13 panels. Added: (1) Gates & Tripwires with hard dates (attribution Jul 19, deliverability Aug 2, mid-quarter recut Aug 30, Day-75 tripwire Sep 19, October CMS re-pull Oct 8); the glance card shows next-gate countdown. (2) Pacing math per mandate (`paceInfo`): need-per-week + linear-expected %, amber "behind pace" flags only after start AND targetsConfirmed. (3) Clay credit ledger (5,000 total; entries in localStorage `creditEntries`; flags at 60%, red 80%). (4) Asks-of-Naveen queue (status cycles open→waiting→done; seeded with attribution, who-owns-what doc, Clay handoff, Genna intro, Bitbucket). (5) Weekly ritual (Mon/Wed/Fri from plan Part G, keyed to `ritualDone[weekKey]` so it self-resets). (6) Onboarding compliance (KnowBe4 due Jul 23, IntraDuction, benefits survey). (7) Relationships expanded to 10 (added Tom Russell, Cheryl Eckel, Melissa Spies, Keith Kennedy) with `lastTouchISO` + "touched today" button; dot goes amber at 7+ days untouched once started (localStorage `relTouch`). (8) `pd()` local-midnight date parser fixes UTC off-by-one. (9) `local.mandateCurrent` merge lets mandate currents persist. Footnote notes engine_state.json stays single source post go-live. Verified headless: 13 panels, interactions persist, 0 errors.
