---
name: keegan-call-asks-built-sep23
description: "Sep 23 2026: Keegan's four asks built and STAGED (former-customer flag on maps and rooms, product filter, generic one-pager builder at /one-pagers/build/, index finder); his Sales Nav search arrived as a URL and became a verified 106-person alumni roster via lemlist People Database + Clay (1,093 credits); deploy maps then rooms by hand; lemlist load is Dallas's call because of the Salesforce sync"
metadata:
  type: project
---

**Trigger:** the Sep 23 2026 call (Otter "GTM Strategy and Automation Review", Keegan, Nathan, Dallas). Dallas: execute everything Keegan wanted, ignore his Claude-makes-up-data worry (he meant untrained users), add a generic PDF creator and a product filter.

**Built, STAGED not deployed (Sep 23 2026), log `automation/logs/keegan-package-refresh-2026-09-23.md`:**
- **Worked-at-a-customer flag.** `motions/keegan/accounts/alumni.py` reads the Clay Enrich Person career histories already on disk from Sep 17 (`_keegan_raw/contacts/`, 123 people) against the active customer list + denylist aliases + story customers, and since the afternoon against Keegan's own past-company LinkedIn org ids (exact). 27 matches (Citizens 11, Hartford 13, Vanguard 3), 14 strong. Orange badge on map cards (map builder takes `alumni_file` on the set), "Warm doors" section on each room, names on the index cards, roster `motions/keegan/alumni/Keegan_Map_Alumni_Sep23.csv`. Zero credits.
- **Product filter.** `shelf.py` pages carry `products` (front / back); `build_kit.selector_section` has a third dropdown; QO, BOO, EH route to the builder with account, industry, reader prefilled.
- **Generic one-pager builder** `motions/shared/execution_kit/generic_onepager/build_generic.py` -> save-rooms `/one-pagers/build/`. Locked template filled in the browser: product x reader x industry, company, optional operation line and platforms named on the call, rep presets, print to one sheet, link encodes the setup. OP bullets are unsourced pattern statements (no figures), SOLVE bullets from the capability register (live) or the roadmap kits (beta, availability on page), proof tile by industry and reader with the own-story guard. Print zoom 0.77 fits three tiles plus the beta line. 13 PDFs pre-printed under `/one-pagers/pdf/<rep>/` and Desktop "Generic One-Pagers - Sep 23" (CardWorks page included).
- **Index** (`build_keegan_index.py`): tools bar with the builder, five product PDFs, a finder over all 122 mapped people (LinkedIn, validated email, account, flags). Lanes heading on rooms softened to "How the plan splits" (Keegan's "this isn't me telling y'all what to do").

**The Sales Navigator search (afternoon):** the saved-search URL encodes the filters as LinkedIn org ids, so `motions/keegan/alumni/salesnav_former_customer_search_sep23.json` IS Keegan's former-customer definition (89 past-company ids incl. Liberty Mutual, American Express, Comcast, Sitel, Sykes; 104 current = his TAM). Clay advanced search expresses the query but its run leg 504s (Sep 22 and Sep 23). The lemlist People Database (`pastCompany` + `currentCompany` + `currentTitle` + `seniority`) returned 509; lemlist matches companies by name, loosely ("Vanguard" pulls Vanguard Facility Solutions), so `gate_former_customer_pull.py` re-checked every row on disk (TAM domain, customer domain or alias, persona title, retail customers only for a contact center role): 113 tier 1. Clay Enrich Person on the 106 new ones: 106/106 verified, 87 on Keegan's exact id list, 83 employer-domain emails (8 personal addresses held back), 106 mobiles, 0 current customers. `Keegan_Former_Customer_Alumni_Verified_Sep23.csv` + Desktop copy. NOT loaded to lemlist: its Salesforce sync would create 106 leads in Keegan's Salesforce; Dallas decides. Dallas's own four lemlist lists (208 People Database contacts, mostly no email, some off-target) reviewed in the log; the "following Intradiem" list of 6 is the keeper.

**Traps:** rooms rebuilt with `--allow-stale` (keegan/vanguard Salesforce record still unread). Both deploys also ship alex, frank and inger pages staged Sep 20 and 21 plus today's theme pass (14 maps files, 22 rooms files differ from live). The builder's `.pvw` preview height is set inline by JS; print CSS must reset it or every PDF runs to two pages. Clay `routines runs get` pages 20 rows at a time; pass `--limit 100` and follow `cursor`. The repo's `coordinator/memory` is an EXPORT of `~/coordinator/memory`; a file written into the repo copy is wiped at the next sync_publish.

Related: [[keegan-package-refresh-sep20]], [[keegan-execution-kit-sep18]], [[keegan-alumni-motion-aug3]], [[department-shelves-sep18]].
