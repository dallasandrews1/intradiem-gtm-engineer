---
name: stars-dwo-messagegen-refresh-jul23
description: Jul 23 applied Naveen's DWO product-description refinements + CSV proof-library expansion across all 5 Stars MessageGen workflow nodes, and fixed the stale table-trigger snapshot
metadata:
  type: project
---

PROJECT (Jul 23 2026): Applied Naveen's Jul 23 Slack-DM direction to the LIVE Stars 5-Touch workflow `wf_0tiegzuo3PzJ4UtUGFA` (nodes E1 `wfn_0tiehe8J6zEXqoGKnGV`, E2 `wfn_0tiehwcEeQthi2Yt2yy`, E3 `wfn_0tiei0jyJuTqKPtrjzT`, E4 `wfn_0tiei41w6ds8kKjs74S`, E5 `wfn_0tiei7eTKfUdXe76BVJ`). Edited via clay MCP `edit_node` (agentPrompt__replace); structure/gates/critics/wiring/per-touch proof assignments all left intact.

**Context:** The QO→DWO platform swap was ALREADY live (Dallas did it Jul 22). This session applied the Jul 23 refinements that were NOT yet in the live nodes.

**Changes (all 5 nodes):**
1. Product one-liner: dropped "shifts/reprioritizes work across queues" (Naveen's "sentence 1 talks about queue optimizer... it actually moves agents across queues and not work" critique). Replaced with Naveen's own language: short = "our Dynamic Workforce Orchestration platform reads the operation in real time and acts the second conditions shift, rebalancing work across queues so service levels hold on the days that usually slip"; fuller = "watches every signal... work rebalanced across queues, coaching and training landed in idle time, schedules flexing before service levels slip, which is exactly where the customer-service and CAHPS points live."
2. Removed the "operationally, and I'm careful not to read that as a Stars number" disclaimer everywhere (Dallas: "adds nothing positive"; Naveen: "lets remove that too"). Cleaned the E1 proof-beat mandate + the shared EXAMPLE A + E1 GOLD STANDARD.
3. Updated all 3 shared EXAMPLES (A/B/C) product lines to Naveen framing (varied, no template creep).
4. Expanded the proof menu: added an EXTENDED PROOF LIBRARY from the website customer-stories CSV, alongside the 6 Humana lines. Hard NAME-CLEARANCE rule: only Humana named; every library result uses its published anonymized descriptor ("a multinational financial services company," "a leading North American bank," etc.), never the real name. Per-touch defaults UNCHANGED (E1/E4/E5 Humana, E3 UHC, E2 none); library is opt-in only when a contact's industry is a clearly better-matched peer.

**Snapshot fix (Dallas's explicit ask), CORRECTED same day:** the clay_table trigger `6c999a95-8ec4-4c5f-8b01-677425d551dc` (Invoke-Workflow-from-table column, field `f_0tihfhlVMZAjmcWgejt`) was pinned to stale snapshot `wfs_0tilld1KekrUXJjaSDh`. First fix set it to `latest`, which BROKE the table handshake (column stopped creating runs entirely, see [[clay-table-trigger-snapshot-pin]]). Final working state: pinned to concrete verified capture `wfs_0tinhdnrXM5dhKRWkxe`. STANDING CONSEQUENCE: every future edit to this workflow must end with re-pinning this trigger to a fresh snapshot or table runs keep the old prompt. Manual trigger `d9fc37a5...` runs `latest` fine. `clay workflows snapshots list/get/restore`.

**Open flags:** (1) "agents vs work" wording resolved to Naveen's own later "rebalanced across queues"; confirm if he prefers "agents/coverage" or wants the explicit "Intradiem's" prefix on first platform mention. (2) Draft Audit (Terra) critic column is a UI-only table column and only knows the Humana/UHC proofs; if Dallas wants ACTIVE rotation into library proofs, widen its approved-proof whitelist or those lines may HOLD. Default Humana behavior unchanged, so low risk now. (3) Voice Fix E1-E5 nodes are orphaned ON PURPOSE: Dallas confirmed Jul 23 he no longer uses the Voice Fix pass now that the MessageGen prompt itself is sound. Do NOT re-flag or rewire them. Supersedes the two-pass VoiceFix architecture in [[voicefix-two-pass-architecture-jul20]] for this Stars workflow. (4) Repo mirror `Clay_MessageGen_SystemPrompt_v2.md` documents the now-unused table column, not the workflow; it is behind. validate_workflow = valid, 0 errors. Nothing sent; new copy applies on Dallas's next rerun of the 143 (he plans refinements-forward, not retroactive to in-flight cohort).

**SMOKE TEST (Jul 23):** ran 2 synthetic rows through the manual trigger (Persona 1 SCAN stars_quality clin=4.0; Persona 2 Geisinger coo_finance) end-to-end; both completed all 17 nodes clean in 42s on the fresh edited snapshot. Live output verified: new DWO line ("reads the operation in real time... rebalances work across queues"), NO Stars-number disclaimer, per-touch proof rotation intact (E1 Humana AHT, E2 none, E3 UHC $190M, E4 Humana capacity, E5 Humana table-stakes), only Humana named. GOTCHA: node 5a requires why_now NON-EMPTY (empty string fails the whole run).

**Finance lead-figure decision (Jul 23):** did NOT lock Persona 2 to addr_2028_musd. Reason: the graded 9/10 Example B (Blue Shield) leads finance with an ANNUAL "$X a year" figure, and the model matched that in the smoke test ($22.7M annual, not the $12.3M window figure). Latent conflict exists (NUMBER DISCIPLINE says lead with the 2028-window figure, but Example B leads annual); reconciling is a positioning call Dallas + Naveen own, not a pre-run blocker. Both figures are real table columns (addressable_forgone_qbp_musd = f_0tigi0jRvx7xNjMUszG), so it's positioning not integrity.

**Draft Audit critic finding (Jul 23):** the live table critic column still lists the OLD product phrasing ("our Dynamic Workforce Orchestration platform shifts and reprioritizes work across queues") as a MUST-PASS example, but its rule is "never HOLD a product-capability description," so the NEW wording passes fine (harmless, just stale). The critic still whitelists ONLY the six Humana lines for customer RESULTS, which confirms: the extended proof library is safe only while Humana stays the per-touch default; widen this critic's whitelist (UI-only) before any active rotation into library proofs.

**ROUND 2 (Jul 23, same session):** Dallas directed that Naveen's 3 extra Humana points + ALL the website customer stories become "a list of what it can choose from," not a reserve. Applied to all 5 nodes: Humana expanded 6 -> 9 lines (added occupancy +4%, 12,000 voluntary time-off hours, 16,000 dynamic work hours/45,000 automated sessions, all from Naveen's 14:56 DM and the CSV); PROOF LIBRARY expanded to 16 published stories; framing changed from "Humana default / library in reserve" to CHOOSE THE BEST-FIT PROOF; per-touch hard locks ("use ONLY line X, do not use any other line") relaxed to DEFAULTS with allowed swaps in E1/E4/E5; no-repeat + one-proof-per-email + E2-light preserved. Name-clearance widened: Humana, Virgin Media and Centrica are nameable (published under their own names), everything else anonymized descriptor only. Also finished the finance reconciliation: Persona 2 now leads with the ANNUAL recurring figure (addressable_forgone_qbp_musd) with December as a qualitative urgency beat, matching graded Example B, both the NUMBER DISCIPLINE and CORE POSITION lines updated across all 5 nodes.

**ROUND 3 (Jul 23 late): cc_ops persona ADDED to the motion.** Dallas found all his cc_ops contacts exiting HOLD_persona (the Jul 19 gate was deliberately scoped to stars_quality + coo_finance, the two personas with written copy; cc_ops sat in the table as a planned segment). Per his call: 2g gate now accepts cc_ops as a third in_icp value, and all 5 MessageGen nodes carry a Persona 3 (ops) routing block, checked AFTER P1/P2: ops titles (Operations/Contact Center/Customer Service/Customer Care/Call Center/Service Delivery/COO) lead with the operational insight (the CS/CAHPS points are decided by how the operation runs day to day, the part they own), star number as support, dollar late as stakes, Humana ops lines (capacity/occupancy/AHT/automated actions) flagged as natural proof fits. Fallback for unmatched titles is still Persona 1. Smoke-tested clean (wfr_0tinq3xeYB4xXsUTUpa, ops-framed E1 verified); table trigger re-pinned to wfs_0tinq38Fup5hxGU2xCT per [[clay-table-trigger-snapshot-pin]].

**HARD DEPENDENCY BEFORE LAUNCH:** the live Draft Audit critic column whitelists ONLY the six original Humana lines and hard-codes each touch's proof in CHECK 3, so it will HOLD Humana lines 7-9, every library line, and any substitution. Complete replacement prompt written to `motions/star_ratings/Stars_DraftAudit_Critic_Update_Jul23.md`. ORDER: update that column FIRST, then rerun/launch. Dallas told Naveen he wants to launch the moment the prompt is updated; 62 contacts remaining.

Related: [[stars-5touch-engine-live-state-jul20]] [[proof-lines-attribute-to-intradiem]] [[messagegen-per-motion-prompts]] [[stars-live-table-dollar-wiring-jul20]]
