---
name: lemlist-standard-sequence-jul31
description: Jul 31 decision - Dallas's Finance-campaign skeleton (adaptive branches, calls, voice notes) becomes the standard 10-day lemlist sequence with five fixes; personalization = Clay-generated per-lead variables injected into approved lane spines, never lemlist AI variables
metadata:
  type: project
---

Jul 31 2026 (evening, after the trial build in [[lemlist-trial-build-jul31]]): Dallas hand-edited Stars - Fresh Pool / Finance (cam_2gy9hmEvMjYEuPZ8A) in the lemlist UI to his vision of the 10-day sprint: heavier multichannel (call tasks, voicemail, LinkedIn voice note, like/follow) on top of the adaptive branch tree. Claude reviewed it against documented outbound evidence (30MPC 10-14-touch template and Gong 85M-email data, Gong 304K CTA study, Lavender 231K benchmark, lemlist/ElevenLabs voice-note data) and ruled:

**His skeleton IS the standard** for both fresh-pool lanes, with five fixes: (1) kill or fill the ~9 blank steps (blank emails/calls/DMs stall or send empty; one carried lemlist's default placeholder voice-note text); (2) voice notes and LinkedIn DMs deliver only to 1st-degree connections, so they live ONLY in the invite-accepted branch; (3) AI voice note uses Nate's CLONED voice only (clone is free, 5 credits per send, trial has 200 credits so accepted-branch only), never a stock voice; (4) interest-based CTA on email 1, time-ask escalates mid-sequence (Gong 304K + 30MPC + Braun converge on this); (5) 3 emails per lead max, each a new angle, ~9-11 total touches over 10 business days with late density (calls double-tap in the back half), breakup day 10.

Evidence note: Lavender's 231K benchmark shows finance personas reward A-grade email craft more than any other persona (79% reply lift), validating the Finance-lane split and craft investment.

**Personalization architecture (the strike-room goal), REVISED same evening: Claude generates directly for the trial, no Clay build at all.** Lemlist AI variables are BANNED for copy (they bypass the verified-claims gate, voice/cadence system, and exclusion discipline). Dallas asked whether Clay was required; it is not. The locked Jul 31 load lists (review doc section 9: 48 Resurrection, 15 Finance, 2 Quality, with plan + qbp_avg per lead) carry all needed per-contact data, so Claude writes the per-lead variables ({{opener_line}}, {{contract_line}}, {{vm_hook}}, {{voice_script}}) in-session through the copy skills and attaches them as customVariables on API lead load; lemlist fails closed on missing variables. Clay-side generation (or a scheduled Claude watch-and-generate job) is a POST-trial paid-rollout decision on receipts; note Clay tables aren't API-readable on the current plan (observability gate), which weakens the Clay path. Auto-land into the campaign stays FROZEN until the [[gate-integrity-fanout-first-run-jul27]] STOP-THE-LINE fixes are done; trial loads stay deliberate API batches after the real-row final gate.

**POST-REVIEW HARDENING (Jul 31, after Dallas's corrections + premortem):** "comparing notes" and the "leverage/lever" family purged everywhere (see [[banned-phrases-comparing-notes-jul31]]); both E1s restored to the house CTA form; both LinkedIn invites rebuilt NOTE-LESS (lemlist API silently drops empty-string message updates, the only way to clear a note is delete + re-add without a message; new invite ids: Finance stp_Fa84LhBJhhopceLaQ, Quality stp_isrRgXBN6XPZniHCJ); voice scripts open "it's Nathan Belfield", no company, all end on a soft question; comment-last-post steps banned from the standard (auto-only, unreviewable public content on Nate's profile) and replaced with a second profile visit. Premortem verdict (moderate confidence): top risks are trial-runway math (Start by Aug 3-4 or the 10-day sequence can't finish by ~Aug 13; fallback = activate Nate's own free trial Mon Aug 3), phone coverage (fresh pool has no phone data, load with findPhone and report coverage %), and Nate's LinkedIn exposure (set daily caps at connect, voice-clone consent required). Full load spec in the stage file's Load notes.

**OPT-OUT PARITY (Aug 1 2026):** all 18 email steps across Nate's three campaigns (Resurrection 4, Quality 7, Finance 7) now carry the same opt-out treatment as Jack's two campaigns: mid-sequence and day-1 emails end "If you'd rather not hear from me, one word and I'll stop", breakups end "Either way, this is my last note" (Resurrection's "closing the loop" gets "This is my last note" to avoid doubling its existing "Either way"). Closes the CAN-SPAM/PECR gap found in the Aug 1 strategy review; every email step in all five rep campaigns is now covered. Campaigns stayed Draft throughout.

**MONDAY-DEMO PREP (Jul 31 late night):** Resurrection brought to standard (invite rebuilt note-less as stp_eLt2WgPsuZziMkCkj, "circling back" cliché purged from the fallback call script and just-tried-you email; no blank steps existed). Identity records recovered from repo sources (Jul 10 eligible-load CSVs + Wave 1 contact list MD + gap-fill CSV): 23 of 48 Resurrection and 8 of 15 Finance leads fully resolvable; Yale (Devoted) held out (source first name is "W."); Quality's 2 not in any repo source. The direct MCP lead-load was classifier-blocked (matches [[classifier-blocks-unattended-automation]]), so the exact load payloads (leads + full customVariables) are staged at `motions/star_ratings/Lemlist_Load_Payloads_Jul31.json` for Dallas to trigger with a paste-back; loading spends zero credits (no enrichment flags), findPhone remains a separate credit decision. Remaining 24+7+2 records come only from Dallas's Clay Contacts export. both fresh-pool campaigns now carry the standard live in Draft. Finance: 3 remaining dead steps deleted (Dallas had already cleared 2 in the UI and added a linkedinCommentLastPost to the no-phone branch, which stays), all scripts filled, E1 interest CTA, variable shells in, delays wired to the 10-day pacing. Quality: rebuilt to the same skeleton (added call 1 / manual voice note / call 2 to the accepted branch, day-after call + double-tap to the has-phone branch, like + follow to the no-phone branch), same shells and pacing. Voice-note steps are manual tasks until Nate's clone exists (swap = delete + re-add with recordMode ai, update can't change recordMode; title char limit is 100). Per-lead variables for all 17 fresh-pool contacts staged in `motions/star_ratings/Lemlist_Variable_Stage_Jul31.md`, passed the gtm-copy-reviewer gate (PASS WITH FIXES, all 8 fix sets applied: de-call-center the "floor/under load" language, Yang and Solis overreach cuts, Point32/CHPW template-creep splits, CTA fixes). 13 of the 15 Finance leads still need first_name/email/title merged from Dallas's Contacts export (they exist only in the Clay table); Miyasato and Lynch are complete. Load order unchanged: Nate channels, Wave 1 export + de-dupe, Dallas eyeballs rendered leads, then API load.
