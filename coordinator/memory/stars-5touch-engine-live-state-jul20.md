---
name: stars-5touch-engine-live-state-jul20
description: "Live state of the Stars 5-touch workflow wf_0tiegzuo3PzJ4UtUGFA after the Jul 20 rebuild: fully inline, generates+gates+assembles all 5 emails, Humana social-proof menu wired in. Workflow owns all 5 emails."
metadata:
  node_type: memory
  type: project
  originSessionId: 7c09b570-5dad-4007-8754-8157e967649b
  modified: 2026-07-21T01:07:08.540Z
---

Jul 20 2026 PM: The Stars 5-touch send-readiness workflow `wf_0tiegzuo3PzJ4UtUGFA` was rebuilt to actually RUN end to end (it validated but every run failed before). Current live state:

**PROOF-LINE VARIETY FIXED + CONFIRMED (Jul 20 PM):** the audit correctly caught E3/E4/E5 all reusing the same Humana AHT line (each touch picked "best fit" from the menu with no cross-touch coordination -> converged). Fix = hard-assign a distinct proof per touch: E1=Humana line5 (first-year in-year return), E3=UHC $190M (its designed proof, NO Humana line), E4=Humana line1 (2hrs capacity), E5=Humana line4 (table stakes), E2=none. Confirmed on a real run: all 4 distinct, verbatim, clean. The Wave 2 Audit table column (combined figure+voice+repetition gate) works; tightened prompt (default PASS, no comma-counting) is in the handoff README §4. Additional copy fixes verified on live rows (Jul 20 late): EMAIL SPACING (paragraph breaks, added to the 5 Voice Fix nodes), MANDATORY proof line (won't drop under word pressure, E1/E4/E5), payer-scoped-dollar audit clarification, and NO STAT REPETITION (E2-E5 reference the gap conceptually instead of restating E1's exact "X.X under 4.0" star stat, they stay substantive + name the contract). The current pinned snapshot changes every edit; the LIVE snapshot id + full state is tracked in `Intradiem GTM Engineer/STARS_WAVE2_HANDOFF_README.md` section A. REMEMBER: re-pin the clay_table trigger (id 6c999a95-8ec4-4c5f-8b01-677425d551dc) to a CONCRETE new snapshot after ANY workflow edit ("latest" won't dispatch); see [[clay-workflow-execution-gotchas-jul20]] rule 9 and [[verified-claims-audit-calibration]].

**CONFIRMED WORKING end-to-end (Jul 20 PM): a full run completed with bodies=5/5** (E1 service-star gap, E2 soft follow, E3 UHC stakes, E4 measure breakdown, E5 short close). The blank-WF-Msg saga is SOLVED. Final architecture = SLIMMED generation-only + a purely LINEAR single-incoming spine with capture code nodes on the spine and a distant-ref Assemble terminal. Never reintroduce a fan-in/join or a 2-incoming threaded capture (both stall the run in `pending` forever). Full node map + the 3 stacked Clay quirks that caused it are in [[clay-workflow-execution-gotchas-jul20]] rules 7+8. Working spine: MsgE1->VFE1->CaptureE1->MsgE2->VFE2->CaptureE2->...->VFE5->CaptureE5->Assemble; captures read their VoiceFix immediately (`get_input("structuredOutputs")["revised_body"]`), Assemble (`wfn_0tihrbzCRBsMN9U26KF`) reads all 5 captures by distant sourceNodeId ref. Capture IDs: E1 wfn_0tihrlsaRcYsJDeGfb4, E2 wfn_0tihrlvUSbZMkd49tEm, E3 wfn_0tihrm0zGXAb62FUrFe, E4 wfn_0tihrm4CeF3kb5z6BZK, E5 wfn_0tihrm8sdQSYyZGfhFU. Runs take ~10 min (10 serial agents); ~40% may hang mid-run, re-run those cells.

**SLIMMED TO GENERATION-ONLY (final Jul 20).** ROOT CAUSE confirmed from a real production run (wfr_0tihqnfJeVFi7tFWpc2, Mel Chapman): Clay AGENT nodes hang ~5% of the time (a ~3s empty dispatch failure, production too, not just CLI). With ~20 agents the run died ~2/3 of the time BEFORE assembling, so WF Msg columns came back empty and the Invoke Workflow cell captured a partial node (a HOLD). Fix: deleted all 36 audit-tail nodes (malformed guards, compose-records, figure-critics, send-ready gates, HOLDs, voice-audits) leaving a 22-node generation-only spine: Trigger -> gates 1g/2g/3g/4/4g -> 5a -> MessageGen E1 -> Voice Fix E1 -> ... -> Voice Fix E5 -> Assemble (`wfn_0tihrbzCRBsMN9U26KF`, terminal, reads the 5 Voice Fix `$.structuredOutputs.revised_subject/body`, outputs msg1-5). ~10 agents now (~60% clean). The figure + voice audits MUST be re-added as TABLE columns over the WF Msg emails (relocated gate; CRO scrutiny). Operating model: run the Invoke Workflow column, re-run cells that don't fill all 10 WF Msg fields (Clay re-does failures). Full handoff: `Intradiem GTM Engineer/STARS_WAVE2_HANDOFF_README.md` (run on WORK laptop in VS Code Claude Code, NOT Cowork). Below is the pre-slim architecture (audits now moved to table):

**Architecture (all inline now):** per touch En: 5a-style compose -> MessageGen En (agent) -> Voice Fix En (agent) -> inline malformed guard (code) -> guard gate -> compose-record (code, reads from 5a spine, emits signal_evidence/top_signal/vertical/source_motion) -> **inline figure critic (agent, automapInputs=false)** -> **inline send-ready gate (code)** -> HOLD_En (code) -> next touch. Then **Assemble All 5 Emails** (wired to the 5 Voice Fix nodes' `$.structuredOutputs.revised_subject/revised_body`) outputs msg1_subject..msg5_body. Every fragile `clay_function` node (malformed guard, figure critic, send-ready) was replaced with inline agent/code. See [[clay-workflow-execution-gotchas-jul20]] for the runtime rules that forced this.

**Decision: the workflow OWNS all 5 emails** (Engine A / the old table Msg1Subject/Msg1Body columns are NOT used for the wave — they point at the old MessageGen Email 1 and stay untouched). Dallas: "email 1 didn't prove anything, it wasn't good enough to get a reply." Wave 2 ships as a full 5-touch off this workflow.

**Social proof wired in (Jul 20):** MessageGen E1-E5 each carry a HUMANA SOCIAL PROOF BEAT block (6 verbatim, us-attributed lines drawn from the Value Repository Humana quotables); the AI selects the single best-fit and places it as the last line before the CTA, varied across touches. The 5 figure critics carry a matching PERMITTED CUSTOMER PROOF addendum so those exact lines pass and anything altered/off-menu FAILs. UHC $190M stays as the separate stakes beat (already in MessageGen), not customer proof. Rule: [[proof-lines-attribute-to-intradiem]]. PROVEN on E1 (real run: Humana "first-year in-year return... bringing us in" landed before the CTA, critic PASS).

**Agent-hang flakiness = CLI-harness artifact, RESOLVED (Jul 20).** The intermittent ~3s empty agent-dispatch hang (hit any of the 20 agent nodes ~1-2/run) appeared ONLY in `clay workflows runs test`. A REAL Centene row run via the Invoke Workflow COLUMN (Clay's production executor) completed clean, all agents, no hang. So the production path is reliable; do not waste cycles chasing this in the CLI. A "resilience decoupling" (critic/gate branches off the generation spine, Assemble reading Voice Fix directly) was tried and then REVERTED: it turned the workflow from one terminal (Assemble) into 6 terminals, and the Invoke Workflow COLUMN captures the terminal AT RUN TIME, so it silently started capturing the HOLD_E5 send-ready node (Send Ready/Voice Verdict/4 structured outputs) instead of Assemble's msg1-5 -> WF Msg columns came back blank on a "Success" cell. Reverted to the serial chain (MsgE1->VFE1->...->HOLD_E5->Assemble) so Assemble is the SOLE terminal and the column captures msg1-5. Lesson: keep Assemble the single terminal; a serial hang fails the run visibly (re-run the cell), which is safer for a send gate than a silent blank. If a rare production row errors, re-run the column (Clay re-does only failed cells).

**Live-run prerequisite learned:** the Invoke Workflow column Inputs must map ALL required trigger keys or the run dies at node 4 "Tokens ready" (fn_tokens_ready, `wfn_0tiehbc7kWMosXAPboG`, requires First Name/Job Title/Company Name/Persona Key/Status/Universe). first_name was initially unmapped -> node 4 failed. Also: Dallas deleted+recreated the First Name/Last Name columns, so the `first_name` input had to be RE-MAPPED (Clay binds by column ID; a deleted column orphans the mapping). New First Name/Last Name formula = `{{Rows from: SalesNav Staging (Persona Pull)}}?.["First Name"]`/`["Last Name"]` (inserted via the / picker so it binds by ID).

**Dallas's remaining table wiring (his hands, UI):** the Invoke Workflow column (19 inputs mapped) captures the workflow output; create 10 `WF Msg1 Subject`..`WF Msg5 Body` extractor Formula columns (`{{Invoke Workflow}}?.msgN_subject`/`_body`), then point the wave-2 Persona-1 sequence at those. Trigger node id `wfn_0tiegzuTFCfeSZXx32R`; entry gate 1g declares all 19 inputs so the column surfaces them. See [[invoke-workflow-trigger-inputschema-jul20]].
