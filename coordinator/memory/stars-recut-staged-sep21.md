---
name: stars-recut-staged-sep21
description: Stars re-cut APPLIED Sep 21, 13 hold leads PULLED Sep 22, bridge fix Sep 22: variables (52 leads) and steps (FIN 8, QUA 14, RES 6 of 7) APPLIED and read back exact; Resurrection variant B refused (UI: pick A); 20-lead hold pull still staged; campaigns PAUSED
metadata:
  type: project
---

Sep 21 2026. All three Stars lemlist campaigns are PAUSED (Finance cam_2gy9hmEvMjYEuPZ8A 13 leads, Quality cam_viEbB6HkYsCPtxKbi 29, Resurrection cam_sh3JCJoxtEHyjGrsw 43). Re-check of all 85 against the CMS re-issue: 13 hold (Clever Care 5, Solis/Athena 3, NYC Health + Hospitals and MetroPlus 3, Clover 2), file `october-flywheel/rehearsal_25v26/Live_Stars_Leads_Recheck_Sep21.csv`. Resurrection leads carry no qbp_avg at all.

Staged, nothing written to lemlist or Clay: `motions/star_ratings/Stars_Copy_Recut_Sep21.md` (qbp_avg retired for a per-parent `measure_read` from the six durable measures; blank read means the lead does not send), `Stars_E1_Prompt_Clover_Delta_Sep21.md`, `WhyNow_MeasureRead_Formula_Sep21.txt`, Polar sheet `motions/shared/polar_sheets/Polar_Sheet_Stars_WhyNow_Clover_Recut_Sep21.md` (task `stars-whynow-clover-recut`, Part A look-only, Part B on hold).

**Why:** the E1 prompt's live Clay home could not be found. The Jul 22 paste-sheet column ids are gone from Contacts (Buying Committee) and `clay workflows get wf_0tiegzuo3PzJ4UtUGFA` returns not_found, yet `Invoke Workflow` f_0tihfhlVMZAjmcWgejt still points at it. The confirmed live carrier of the old thesis is formula `Why Now (2026-cycle)` f_0thuvhooGA2y8idyaZQ.

**How to apply:** order is Part A report, Nate reads the copy page, Sep 4 pressure pass on the untouched Stars steps, then one lemlist write plus Part B in the same sitting, campaigns still paused. lemlist search_campaign_leads fails above limit 10 (proxy error), page in tens. Opener rule that governs: insight first, never their own number (E1 prompt line 33 and first-draft-engine Gate 1). Related: [[clover-ruling-stars-measure-thesis-sep21]], [[stars-refresh-rehearsal-sep21]].

**Later Sep 21 2026:** a second read-only Clay sweep found `wf_0tiegzuo3PzJ4UtUGFA` unreachable by every CLI route (get, graph, snapshots, list --limit 200). The three "Stars QBP Updated Messaging" campaign tables return "not supported by this API", so they are the only place a prompt could still hide; Polar Part A now asks only that plus the Invoke Workflow run setting. lemlist: live sequences backed up (54 steps) and a 29-step change set built from the LIVE copy, which differs from the Sep 3 page (Quality Email 2 carries the Humana two-hours line): `motions/star_ratings/stars_clover_recut_sep21_CHANGES.json` plus `_PREVIEW.md`, builder `build_stars_recut_changes_sep21.py`. It folds in the Sep 4 pass on the Stars call steps. lemlist's update_sequence_step requires a preview and Dallas's explicit yes per its own contract, so nothing is written until he approves; apply order is measure_read variables on the 72 sendable leads first, then the 29 steps, campaigns paused throughout. Per-lead vm_hook and voice_script were still being checked for old-thesis wording.

**Per-lead variables (Sep 21 2026):** the old thesis is also in the lead variables: 31 of 42 vm_hook and 28 of 42 voice_script values on Finance and Quality leads name a stale rating or say "scored on live calls". Staged fix `motions/star_ratings/stars_lead_variables_recut_sep21.csv` (measure_read on 72 leads, 30 vm_hook replaced, 7 kept, 37 voice_script replaced, 13 hold leads untouched). Lesson: in lemlist a copy correction has three layers, step text, per-lead variables, and the lead list; check all three.

**APPLIED Sep 21 2026 on Dallas's explicit approval.** Variables: 72 of 72 leads read back exact. Steps: 31 operations ok (29 steps plus variant B on the Finance and Quality Email 1s), 46 of 46 fields exact, step counts unchanged, nothing outside the plan moved, every campaign read "paused" throughout and stays paused. Main-session spot-check through lemlist's read-only preview confirmed the new copy live. Left alone: Resurrection "just tried you" variant B (dormant, winner A), the breakup emails, the two STRAY steps. Before any restart: pull the 13 hold leads by hand, Nate's read, Lane 2 send-time re-check. Working method that held up: lemlist writes go through subagents with fixed plan files because update_sequence_step returns the whole step list per call; preview_sequence_update with a dummy newMessage is a cheap read of a step's live text.

**Verified Sep 22 2026 (live lemlist read):** all three campaigns still paused, re-cut copy live on every step (Email 1s open on the CMS recalculation insight, measure_read second, Sep 4 call pass in). The 13 hold leads were PULLED later on Sep 22 on Dallas's approval (Finance 3, Quality 2, Resurrection 8; 72 remain: 10 / 27 / 35; all 13 read back 404; log automation/logs/stars-hold-pull-2026-09-22.md). The release-week cohort shell (movement_read) is still HOLD; no record of the Sep 22 cadence call outcome in Slack or meeting capture, so ask Dallas what was decided before touching it.

**Bridge audit Sep 22 2026 (live lemlist read of all three campaigns).** The re-cut copy is current and accurate but does not close the link from Intradiem to the plan's rating, and the asks name no payoff. Three defects: (1) of the 72 sendable leads only 27 have a `measure_read` naming Customer Service, while 26 name only Getting Appointments, Getting Needed Care or Care Coordination, which a service center does not move, so those leads read as a mismatch to a quality leader; (2) the only proof is efficiency (Humana handle time down 45 seconds, two hours per agent per month), nothing says members had a better experience, so the reader must make the leap; (3) the Finance ask offers a cost-vs-bonus conversation the stale QBP model cannot support, the Quality ask offers an answer that exists nowhere in writing, and the B variants promise a short note that was never built. Also open: whether "I'm not making a Stars claim" stays, since it is honest and also cuts the link the email just opened. Dallas said Sep 22 not to wait on the cadence call outcome; it is not a gate. Fix runs in a fresh Fable session per [[naveen-gtm-physics-stars-relaunch-sep21]] framing; Nate's read of the copy page is still a gate before any restart.

**Bridge fix STAGED Sep 22 2026 (nothing written).** Folder `motions/star_ratings/bridge_fix_sep22/`, log `automation/logs/stars-bridge-fix-2026-09-22.md`. Routing rule: read every durable measure under 4 on the parent's largest under-4 contract (`low_durable` column), not the two lowest; Customer Service under 4 keeps the Stars angle (52 leads: Finance 8, Quality 20, Resurrection 24), Customer Service at 4+ holds for October (Devoted 9, Cambia 5, Baystate 1), and risk-adjustment, HEDIS and network-ops seats hold regardless (5). No "different angle" lane built: it needs its own campaign shell and the October release re-cuts every read in two weeks. `measure_read` now carries Customer Service first and Rating of Health Plan only if under 4; new `contract_id` variable replaces "your largest Medicare Advantage contract" (wrong for Point32Health, Florida Blue, Lumeris). The bridge is the survey wording (CMS MA & PDP CAHPS QA P&TS V15.0, Appendix K: two of three Customer Service items are about the call, the other three composites are about the doctor's office), not a claim; "I'm not making a Stars claim" is retired for "The survey side has no number behind it yet". Finance ask is Humana's return math, no bonus model. The B-variant note exists (note 12 in Offer_Notes_Aug3.md, supersedes note 3). Apply order: Nate reads the page, Lane 2 re-check, variables plan, then step plans, then the 20-lead hold pull, each on Dallas's yes, campaigns paused throughout.

**Variables APPLIED Sep 22 2026 on Dallas's explicit yes (file `stars_bridge_plan_leads.json` only).** Subagent wrote measure_read, contract_id and (Finance, Quality) vm_hook and voice_script on 52 leads; 52 read back exact, campaign state paused before and after; main-session spot check of one lead per campaign matched. Result `stars_bridge_apply_RESULT_leads.csv`, log anchor `evt: stars-2026-09-22#bridge-variables-applied`. Still staged, each needs its own yes: step plans FIN (8 ops), QUA (14), RES (7), then the 20-lead hold pull. Steps must follow variables, never precede them. Note: leads still carry unused pre-recut variables (qbp_avg, opener_line, contract_line); no live step references them. julee.smith@wpsic.com is unsubscribed in Resurrection.

**Steps APPLIED Sep 22 2026 on Dallas's explicit yes (the three step plan files only).** Three subagents in parallel, one per campaign: Finance 8 ops, Quality 14, Resurrection 6 of 7, every sent field read back exact in the main session against the AFTER captures, step counts unchanged, nothing outside the plans changed, all three campaigns paused before and after. The Resurrection just-tried-you variant B (stp_afdYJbTbPXLZkvxkC) was refused again with `SEQUENCE_AB_CAMPAIGN_RUNNING` even though the campaign reads paused; abTest is "A" so the old half-star B is not sending, but it must be fixed in the UI (pick A as winner or paste B by hand) before restart. Lesson: subagent op counts in their reports were off by one on two files while their CSVs were complete, so always recount from the result file, not the report. Captures: `Lemlist_Sequences_BEFORE/AFTER_Bridge_Sep22_{FIN,QUA,RES}.json`. Still staged on its own yes: the 20-lead hold pull (`stars_bridge_plan_hold_leads.json`). Gates before restart: Nate's sign-off read, hold pull, Lane 2 re-check, stray steps and variant B in the UI, Clay E1 prompt.

**Bridge fix APPLIED to lemlist Sep 22 2026 on Dallas's explicit yes; campaigns paused throughout.** 52 of 52 lead variable sets, 26 of 26 step updates and 3 of 3 variant B updates written and verified on real reads; zero collateral drift (54 steps before and after, every unplanned step byte-identical to the pre-write backup `october-flywheel/rehearsal_25v26/Lemlist_Backup_PreBridge_Sep22_<cid>.json`). Log `automation/logs/stars-bridge-applied-2026-09-22.md` (evt: stars-2026-09-22#bridge-applied). **Mechanism correction worth keeping: the lemlist MCP connector is not always exposed to a Claude Code session even when claude.ai shows it connected; the REST API works with the key in `automation/config/lemlist.env`. `PATCH /sequences/{seqId}/steps/{stepId}` is a TRUE PARTIAL PATCH (only fields sent change, but `type` must be in the body), which corrects the Sep 21 belief that a step write rewrites the whole step list (that was the MCP's response shape). Variant B has its own endpoint `PATCH /sequences/{seqId}/steps/{stepId}/ab-test`, and it accepted the Resurrection variant B the MCP refused on Sep 3. Rate limit 20 req / 2 s. Lead variables read back only via `GET /leads?email=&campaignId=`, not `GET /leads/{id}` (404) and not the campaign leads list (skeleton rows only).** NEW OPEN DEFECT: Resurrection lead `lea_TEjXdqf6cQpksTA3v` is filed as Zing Health with email `zsole.munoz@myzinghealth.com`, but the real contact is `munozz@sutterhealth.org`, Director Operations at Sutter Health Plus, so on restart a Sutter director would get a Zing Health Stars email that never bounces; pull it, making the hold pull 21 not 20. It is the only plan-vs-real email mismatch among the 52. Retired variables qbp_avg, opener_line and contract_line still sit on leads carrying the pre-Clover thesis but are referenced by zero live steps.

**Closeout Sep 22 2026.** Nathan's approval relayed by Dallas, so Nate's read gate is CLEARED. Lane 2 re-check DONE and recorded in `motions/star_ratings/bridge_fix_sep22/Lane2_Recheck_Sep22.md`: every public claim confirmed against the Jun 17 2026 HPMS memo, CAHPS QA P&TS V15.0 Appendix K and the Jul 22 2026 re-issue, corroborated by Healthcare Dive Jun 18 2026; the recalculation was strictly one-directional (a contract took the new rating only if higher), and the copy correctly never states what will count in the October 2026 release. **That re-check expires on the October release, due within two weeks, which re-cuts every measure_read.** A scan of all 26 untouched live steps found zero pre-Clover thesis, so the re-cut is complete across the live copy. STILL OPEN: (1) **the hold pull did not happen** despite Dallas reporting it, verified on a fresh read (still 10/27/35 = 72 leads, all 20 holds present, the Zing/Sutter defect lead still live) and the Salesforce-sync revert in [[lemlist-salesforce-sync-overwrites-lead-fixes]] is the likely cause, so re-verify on a real read rather than trusting the UI; (2) the two self-labeled stray steps could not be deleted because the auto-mode permission classifier refuses DELETE as an external system write, same class as [[claude-code-auto-mode-blocks-deploys]], so it needs a Bash rule or the UI; (3) Resurrection variant B now holds correct copy with A as selected winner, safe as-is. Log `automation/logs/stars-bridge-closeout-2026-09-22.md`.

**LIVE Sep 22 2026.** Dallas started all three Stars campaigns; the bridge fix is done and the state is verified clean. Hold leads were neutralised by **pausing each lead in place, not pulling them** (20 of 20 isPaused, including the Zing/Sutter defect lead at 19:01:56), which is better than the staged pull because the leads and variables survive for the October rebuild and counts stay 10/27/35. 51 of 51 keep leads active. No Stars email has sent (all of today's emailsSent belong to the unrelated NL Planners campaign); the only Stars activity since going live is a harmless LinkedIn profile visit on a correct keep lead. **Lesson: verify a lead fix by `isPaused`, not by lead count — my closeout log wrongly reported "the pull did not happen" because it counted rows instead of reading paused state.** Still cosmetic-only: the two empty terminal stray steps remain (auto-mode classifier refuses DELETE as an external write, so UI or a Bash rule). Live risk: the Lane 2 re-check expires on the CMS October 2026 release, inside two weeks, which re-cuts every measure_read in the now-sending copy. Log `automation/logs/stars-bridge-live-2026-09-22.md`.
