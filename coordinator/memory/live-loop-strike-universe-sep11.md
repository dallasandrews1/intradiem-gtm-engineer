---
name: live-loop-strike-universe-sep11
description: "Sep 11 2026 live loop, spec'd and built in dry-run: strike universe from an Audiences segment at build time (0 credits), war-room staged signals served as unreviewed with APPROVE/DENY replies in the rundown thread (no new Slack scopes), nightly deploy behind a flag (off), weekly account-facts refresh (plist not loaded)"
metadata:
  type: project
---

Dallas's Sep 11 2026 call after the seed swap: static hand-saved files are not the goal, a live loop is. Spec: motions/live_loop/LIVE_LOOP_SPEC.md; page: ~/Desktop/Intradiem Deliverables/Live Loop - Strike Universe - Sep 11.html.

**Built (dry-run, nothing deployed, no credits, no gate flipped):**
- Universe: saved Audiences segment "TAM Strike Universe (engine)" audseg_0tl7vtuy9dbuQWxtJRK (Prospect, employee_count >= 1000, ICP industries, and any of CC read within 12 months / 6sense 6QA / heat lane rep or cohort; 354 on Sep 11). `tam-outbound-engine/universe_from_audiences.py` reads it at build time and regenerates tam_accounts.csv / sellers.csv, merges triggers.csv (existing rows + facts ledger + approved signals, keyed by domain/type/date, never deletes). Config `config/universe.json`; owners `config/sf_owners.json` (only Keegan 005V5000001gAGSIA2 and Centene->Nathan mapped); facts ledger `data/account_facts.json` seeded from the Sep 11 research. Provenance per row: filing fact when fresh (<= 90 days) else the Audiences employee_count cited as such; agent_count public or banded ESTIMATE. Fails closed (Clay error = last CSV kept, snapshot errors["universe"]).
- Signals: `automation/signal_review.py` (sync / decide / resolve / pending) ingests automation/logs/staged_signals.csv into automation/config/signal_reviews.json with stable ids `sig-YYYYMMDD-slug-hash`, exact-name domain resolution (Capita must not become Capital Group), scored classifier that needs the verbatim quote itself to match a family (war-room angle lines say "NOT a QBP signal" and fooled the first version), customer parking by denylist or SF Account Type. Generator attaches `unreviewed_signals` to strike rows (context only, never scoring, never copy); approved ones become cited triggers at the next build. Sep 11 real run: 3 awaiting review (Centene x2, Cambia), 49 parked with reasons.
- Slack: per Dallas the Slackbot app stays single-scope mcp:connect, so approve/deny rides the existing rundown thread listener: run_daily_rundown.sh carries "N signals awaiting review" from signal-review-<date>.md; run_rundown_thread_listener.sh handles APPROVE/DENY <id> by calling signal_review.py decide (its only new allowed write). Rundown SKILL.md source list change is in proposal_ledger, not applied.
- Nightly: automation/config/brain_publish.json {deploy:false, signal_review_sync:true, universe_rebuild:true}; sync_publish.sh runs the ledger sync and universe rebuild before the snapshot and passes --deploy only when the flag is on.
- Weekly refresh: run_account_facts_refresh.sh + com.dallasandrews.gtm.accountfacts.plist (Sunday 06:00, NOT loaded), prompt automation/prompts/account_facts_researcher.txt, merge automation/account_facts_merge.py (dry-run until config merge=true; validated 18/21 of the Sep 11 research on a dry run).
- Tests: universe 15/15, signal review 12/12, engine 60/60 (seed fixture), brain 41/41. Scratch snapshot: 342 accounts, 0 seed, 12 customer-excluded, universe=audiences, no errors. Live published snapshot still the 16-row Sep 11 set until deploy is flipped. The 16-row files are preserved in tam-outbound-engine/tests/fixture_sep11_sourced/.

**Gotchas:** signal_review's first run wrote prefix-matched domains into automation/logs/.heat_domain_cache.json (shared with the heat scorer); cleaned, and sync now takes --domain-cache for tests. With the 12-month read rule Cambia fell out of the universe despite a fresh trigger; the filter is Dallas's lever. 298 of 342 rows are Tier 3 because agent counts are estimates.

**Dallas's hand, in order:** shape the segment filter in Clay; fill sf_owners.json; reply to tomorrow's rundown signals; flip deploy; load the weekly plist and later set merge=true. Related: [[tam-universe-swapped-real-accounts-sep11]], [[brain-render-wrong-repo-binding-sep11]], [[signal-marketing-loop-strategy-aug25]].
