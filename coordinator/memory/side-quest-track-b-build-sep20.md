---
name: side-quest-track-b-build-sep20
description: "Sep 20 2026: side quest Track B built on sample data in motions/ai_adoption_sidequest/: sources/ adapter pattern (Greenlight + Microsoft 365 Copilot), Delivery_Contract spec page, Kathryn's CSA v1.0 as a self-scoring intake page, offer sheet with price blanks for Derek; 515 checks green, cinematic deck staged, earlier build live; NOT deployed, uncommitted on main"
metadata:
  type: project
---

Built Sep 20 2026 after Kathryn rescinded the no-commercial-product ruling (see [[side-quest-productized-offer-thinking-sep20]]). All in `motions/ai_adoption_sidequest/`, sample data only, nothing deployed, changes uncommitted on main.

1. **Adapters.** `adoption_engine/sources/` + `config/sources.json`. Engine now runs `--source NAME` or `--all` (writes one state per tool plus `data/portfolio_state.json`, which never blends tools into one rate because units differ). Greenlight state verified identical to pre-refactor. Microsoft 365 Copilot adapter is modeled on Microsoft's real report (headers checked against Microsoft Learn Sep 20): the report is a daily SNAPSHOT of last-activity date per app, not a usage log, so the unit is active app-days, pulls must be daily, and user names arrive concealed by default (adapter refuses with the fix). Loader refuses any non `@sample.invalid` roster id while a source's `sample` flag is true; that flag in sources.json is now the only live/sample switch (replaces the old "flip it in score()" step).
2. **Delivery_Contract.src.html.** Spec only: record out, receipt back (maps onto the responses feed the engine already reads), three targets (manager card, enablement queue, in the flow of work), six questions for Product. Makes no claim about what the Intradiem platform can take in.
3. **Current_State_Assessment.src.html.** CSA as `config/csa.json` (Kathryn's wording verbatim) + `csa_scoring.js` (same file inlined in the page and run under node). Three items marked PROPOSED for Kathryn: plain-word stage labels replacing ADKAR terms, 5/3/1 status-to-rating conversion for Sections B, E, A2, per-dimension risk bands. One parenthetical naming two ADKAR stages was cut from her High Risk rollout text. Overall score is withheld until all nine dimensions score. Answers stay in localStorage; page sends nothing.
4. **Adoption_Offer_Sheet.src.html.** For Derek: Assess / Run / Orchestrate, price a literal blank per tier, no Greenlight mention, durations marked proposed.

**Why:** proves "all their AI tools" with a second real-shaped adapter instead of a slide, and gives Joey, Kathryn, Product and Derek each one concrete thing to react to.

**How to apply:** scope is NOT changing officially (Dallas, Sep 20): charter untouched, everything built product-grade, the offer is the reveal at the executive presentation. Next moves: Joey aligned verbally (not a charter change), Kathryn's review of the three proposed items, the Greenlight export by mid-October, Product's six answers as an ordinary conversation with Chris Busbee, the Derek session showing dashboard and assessment only. The offer sheet stays unsent until the presentation unless Derek raises productization himself. No customer contact before then. Dashboard done later Sep 20: tools strip from portfolio_state.json doubles as the tool switcher, unit printed per card, no blended rate; `verify_dashboard.js` runs the BUILT page under node against the engine for both tools at config and two changed-threshold settings (349 checks). That check caught a defect dating from Sep 9: the page rebuilt response records from the fired list, so under a changed threshold a logged response vanished and a responded alert read overdue; fixed by carrying the whole responses feed in the state. The LIVE adoption-telemetry.pages.dev page still has that defect until a redeploy.

**Finish pass, late Sep 20 (Dallas: Joey and Kathryn are partners and see everything; get it share-ready to impress ELT).** Loop closed on sample data: engine emits `state.deliveries` per the contract plus `receipt_to_response`; `Manager_Cards.src.html` is one card per manager ACROSS tools plus an enablement queue, with a browser-kept response log and CSV out in the responses-feed shape; `verify_manager_cards.js` proves a logged response flips the alert to responded in the engine. `Adoption_Briefing.src.html` is the team front door and the exec presentation skeleton (eight slides, pilot numbers as "with the pilot" slots, strength framing, internal tool not named; its last section is team-only and comes out before it goes wider). Shared nav across the five working-set pages; the briefing and offer sit under /team/, unlinked from the set. `stage_deploy.py` staged everything into `~/Desktop/Intradiem Deliverables/deploy-adoption-telemetry/` and keeps the two live paths (/ and /contract/). Suites: 31 + 35 + 24 + 25 + 349 + 21 = 485 checks green. Open decision for the team: what the internal tool is called in front of leadership (one `label` in sources.json renames it everywhere). Dallas deployed that state himself on Sep 20; live and checked at adoption-telemetry.pages.dev (/, /team/, /team/offer/, /managers/, /assessment/, /delivery/, /contract/). Share link for Joey and Kathryn: /team/.

**Cinematic present mode, late Sep 20 (briefing only).** After comparing against Chris and Matt's all-hands deck (see [[product-allhands-deck-design-read-sep20]]), the briefing's present mode was raised to that bar while the working pages stayed calm: eleven slides, dot field, animated five-step loop with a travelling pulse, two LIVE slides framing the real manager cards and the dashboard threshold panel via a `?embed` switch (chrome hidden, nothing else changes; the pages are identical without the parameter), hero and loop numbers read from a compact engine brief, the four pilot numbers stay "with the pilot" slots until a tool's `sample` flag goes false and then fill from that tool only. Stayed on the Jul 30 kit (no lime, no Open Sans), 155 KB, no external requests, reduced motion returns before the animation loop starts. New config field `label_leadership` ("Internal AI assistant" for Greenlight) is what a framed page shows, so the deck never names the internal tool; the wording is still the team's call. `verify_briefing.js` 28 checks. Suites now 31 + 35 + 24 + 25 + 350 + 22 + 28 = 515 green. Staged, NOT redeployed: the live /team/ still has the earlier eight-slide deck until Dallas runs the wrangler command again (auto mode blocks wrangler, and customer data must never sit on the personal Cloudflare account). Mem0 quota was exhausted Sep 20 (resets Oct 1), so this exists in files only. Related: [[side-quest-measurement-layer-sep9]].

**Showing the engine (Sep 21 2026).** Dallas asked how to show it working. Three ways: the dashboard's threshold panel (browser, verified equal to Python), the manager card response log (browser), and `adoption_engine/demo.py` (terminal, four narrated acts on a scratch copy; `--pause`, `--leadership`). The verifier output also reads as plain-English proof (`node verify_manager_cards.js` prints a LOOP CLOSED line).

**Honest bar check (Sep 21 2026, Dallas asked if this is excellent as-is).** Verdict given: show Joey and Kathryn now; do NOT submit or put in front of ELT as-is. The gap is not craft, it is evidence and ownership: (1) zero real data, and the sample generator itself makes coached users recover, so every lift or effectiveness figure on sample data is an artifact of the generator and must never be cited; (2) thresholds, the 5/3/1 scoring and playbook actions are Claude's placeholders carrying Joey's and Kathryn's names before they have shaped them; (3) adapters have never met a real export (the Greenlight shape is invented), so 'one data swap from a pilot' was an overstatement, expect about a week of adapter work; (4) no access control: the manager picker shows every manager, fine on sample data, a real problem with real employee usage; (5) real rows need a company-owned home first, never the personal Cloudflare account; (6) the flow-of-work tier is unconfirmed with Product. Recommendation: stop building, spend the remaining weeks on the pilot; next build work should be driven by contact with real data. For ELT, show less: the deck plus the two live pages, everything else is appendix.

**Expressive present mode (Sep 21 2026).** Dallas asked for the deck to be as dynamic and expressive as Chris and Matt's. Built, briefing only, twelve slides, 176 KB, no libraries, Jul 30 kit: a workforce-grid background that lights up per slide (`data-lit`) with orange at-risk people, word-by-word headlines with a shimmer on the accent phrase, 3D card entrances, a story slide (one person: rise, drop, trigger, manager card, response, lift), an engine slide (two export shapes into one engine, same signals out), rising tiers, shimmering pilot slots, a Contents overlay (`c`), pointer parallax, a closing slide. Both illustrations are labelled "Illustration, not data" and carry no percentages. Found: the earlier dot field had never been visible (shared template paints opaque slide backgrounds), and the SHARED `_tpl/base.css` has a reduced-motion bug that shows every slide at once so only the last is seen; fixed locally in the briefing, still OPEN in the shared file (affects the dashboard's deck and every other page on that template; fixing it means rebuilding and redeploying those pages). Headless Chrome freezes CSS animations mid-way, so end states are checked with `--force-prefers-reduced-motion`. `verify_briefing.js` now 35 checks; suites total 522. Staged, NOT deployed.

**Deployed Sep 21 2026 by Claude on Dallas's explicit ask** (`npx wrangler pages deploy . --project-name=adoption-telemetry --commit-dirty=true --branch=main --force`, from the deploy folder). This session was NOT blocked by the classifier, unlike the auto-mode sessions on Sep 20. Verified after: newest wrangler log has no Workers `wrangler deploy` line, all seven paths return 200, /team/ serves the twelve-slide expressive deck with frame paths rewritten to /managers/?embed=1 and /?embed=thresholds. Correction owed and made: the command handed to Dallas on Sep 20 omitted `--force`, against his standing rule; every command for this project carries it from here (stage_deploy.py still prints it without --force and should be fixed).

**Leadership link (Sep 21 2026).** Dallas asked whether the ELT link had the new deck: it did not, because no leadership link existed. /team/ has the new deck but also the team-only section and the offer sheet link; / is the dashboard with its own older five-slide deck. Built `Adoption_Briefing_Leadership.html`, derived at build time from the one briefing source with `<!--TEAM-ONLY-->` blocks cut, routed to /briefing/, verified identical deck, brief and scripts and no path to /team/ or the offer. STAGED, NOT deployed as of this note; needs Dallas's go. Three links, three audiences: /team/ (the three partners), /briefing/ (leadership), / (everyone with the dashboard link). Still open for a leadership click-through: the working pages name Greenlight (one `label` line in sources.json).

**How to send it (Sep 21 2026).** Recommendation given: do not make slides the page default; send the link with `#present` on the end (`/team/#present` for Joey and Kathryn, `/briefing/#present` for leadership). First open lands in the deck, Esc drops them onto the document with the working pages and, on /team/, their six calls; a default-to-slides page would get in the way on every return visit. Consistent with [[feedback-first-layer-for-non-technical-readers]].

**Slack draft for Joey and Kathryn (Sep 21 2026).** Drafted in Dallas's voice for the group DM C0BNR30H5BQ, leading with /team/#present, flagging sample data and the placeholder calls himself, pointing them to the six calls at the bottom of the page. Not sent by Claude. Note from reading the DM: nothing was posted there between Sep 9 11:17 and Sep 11, so the Sep 9 dashboard link may never have been shared with them; this message should not assume they have seen anything.

**Second deploy, Sep 21 2026 (Dallas: "deploy").** Published with --force: /team/ now carries the reworded bottom section, and /briefing/ (the leadership page) is LIVE for the first time. Feedback from Dallas that drove it: "calls that are yours and not mine" draws a line between teammates; say it as placeholders Claude put in to keep moving that need the person closest to them. Applied to the page heading and the Slack draft.

**Start here block (Sep 21 2026).** Dallas asked whether Joey and Kathryn would understand what was built from the slides; honest answer was they would get what it does but not what is real versus drawn, because the deck is an ELT pitch and builder vocabulary leaked in. Built a team-only Start here block at the top of /team/ (real today, a drawing, three clicks with what to watch) plus a try-it line on the threshold live slide, plain words enforced by a check. Leadership page proven byte-identical to the live copy with `cmp`. STAGED, not deployed as of this note. Standing recommendation: still walk them through it live for fifteen minutes; the block supports that, it does not replace it. Lesson for future pages: a deck built for executives does not orient teammates, who need what is real, what is a drawing, and what is needed from them.

**Third deploy, Sep 21 2026 (Dallas: "deploy").** Start here block and try-it line are LIVE on /team/; /briefing/ confirmed byte-identical after the deploy. All eight paths 200, ran on Pages. Link to send Joey and Kathryn: https://adoption-telemetry.pages.dev/team/#present, with the Slack draft (placeholder wording, not "yours and not mine").

**Sent, Sep 21 2026.** Dallas sent the DM to Joey and Kathryn in the group DM (C0BNR30H5BQ) with https://adoption-telemetry.pages.dev/team/#present. Build work is paused by recommendation; what moves it now is theirs: Kathryn's read on the three proposed items, Joey's export with Jason Dowden (target mid-October), playbook wording, the tool's name in front of leadership. Still to do by Dallas: tell Joey in person about going bigger, the fifteen-minute live walkthrough at the biweekly, the Derek session. Open and parked: /briefing/ is live but unsent, the shared _tpl reduced-motion bug, and every repo change from Sep 20 and 21 is uncommitted.
