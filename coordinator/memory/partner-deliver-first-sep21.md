---
name: partner-deliver-first-sep21
description: "Sep 21 2026: the #gtm-partner-briefs poster went deliver-first; a 12 Minute Meeting crossing gets a brief and one-pagers built before anything posts; Fidelity is the first build, staged not deployed, held on an unread Salesforce record"
metadata:
  type: project
---

Built Sep 21 2026 on top of [[partner-event-poster-sep20]], after Dallas agreed the ask-first stage posts were the wrong shape for a channel Naveen and Haresh now read.

- Flow: `run_partner_events.sh` calls `run_partner_deliver.sh` first (`partner_deliver.py --sync`, then `--apply`), then the poster. Manifest `motions/partner_channel/briefs/deliver_manifest.json`, statuses queued, held, staged, deployed. `--sync` flips staged to deployed by itself once both URLs answer 200. Config block `deliver` in `automation/config/partner_events.json` (`live: false`, `require_record_read: true`), plus `stage_group_max`.
- Poster: a non-pilot record at Delivered 12 Minute Meeting with no deployed asset goes to a BUILD QUEUE log section and posts nothing. With an asset the post carries `[Account brief] · [one-pagers]` and asks nothing. Two or more stage events in a run go out as ONE message; the log shows one `key:` plus a `members:` line, and `--mark-posted` on that key marks every member, so the unattended posting prompt did not need editing. Weekly cap now counts messages (distinct ts), not keys. `--replay --assume-record-read` previews a deliver post.
- Customer gate: union of the Salesforce customer segment, the Jul 10 active customers export, the denylist and the install base. Category "Expand" on a pre-pipeline record is the partner's own category (83 of 251 records), not a customer flag.
- Fidelity Investments (record "CapGemini - Fidelity", AE Keegan Sanders): brief JSON `briefs/data/fidelity.json`, partner-safe page staged at `deploy-partner-pilot/briefs/fidelity.html`, two one-pagers (service-operations, workforce-platform) staged under `deploy-save-rooms/fidelity/frank/one-pagers/`, claims in `kit/CLAIMS_LEDGER_frank.md`. Lead finding: a LIVE Fidelity posting (jobs.fidelity.com 2127334, posted Aug 20) says "Fidelity is building a modern, in-house Workforce Management platform" at "its current design stage". Build-in-house account: alongside, never instead of.
- Research hygiene that mattered: the three signal-researcher passes disagreed (800 vs about 1,000 cuts; 3,300 vs 5,300 hires; 9.8M calls as half-year vs quarter), two cited job postings were 404 by the time they were opened, and a "25,000 employees" figure was not in the article cited for it. Only lines re-opened through WebFetch went on a page. WebFetch is a small model's reading, so a person re-opens the three Fidelity pages before a customer sees a one-pager.
- OPEN: (1) nothing deployed; `automation/deploy_rep_pages.sh pilot` (new target) then `rooms`. (2) `frank/fidelity` is registered with the freshness gate and reads MISSING, so the post is held until the record page is read and marked. (3) No name below the executive layer is live-checked; five profile-only names are internal-only. (4) Amerihealth Back Office and BCD Travel are queued, not built. (5) The unattended build session was DENIED by the auto mode classifier ("Create Unsafe Agents"); builds are attended until Dallas adds a Bash rule himself.

**How to apply:** "build the deliver-first package for <slug>" is the attended task: three researcher passes, re-open every line used, JSON on the Ally schema, `build_briefs.py <slug>`, a shelf block in `shelf_frank.py`, `build_shelf_generic.py`, ledger rows, then `partner_deliver.py --stage <slug>`. Never flip `deliver.live` or deploy without Dallas's explicit ask.

**Sep 21 2026 evening, packages two and three staged (nothing deployed, nothing posted):**
- AmeriHealth Caritas (record "C1 - Amerihealth - Back Office", AE Keegan Sanders). "Amerihealth" was read as AmeriHealth Caritas because ConvergeOne is its reseller of record on the 3xG sheet; added to `name_fixes`, so the war room now sweeps the right company. Frank's word overrides. Lead lines: Louisiana's remaining plans absorb as many as 580,000 enrollees by year end (KALB Sep 20 2026); dual-eligible markets live in 2026; PerformRx winds down Dec 31 2026; new Medicaid eligibility rules Jan 1 2027. Internal only: the NC plan's own Known System Issues Tracker (Sep 16 2026) names a failed robot job, an automation fault open since Oct 8 2025 and manual errors as root causes of wrongly denied claims; read directly from the PDF text.
- BCD Travel (record "Call Design EMEA - BCD Travel", AE Simon Bland, registered Sep 8 and at 12 Minute Meeting by Sep 20). Lead line is BCD's own blog, Sep 17 2026: "ongoing workforce challenges and a need to attract and develop new frontline talent." Genesys Cloud per the vendor's customer story (7,000 people, 57 countries, 75,000 routing queues cut 60 to 70%); the vendor is named on the brief, never on a one-pager.
- Research hygiene again earned its keep: a quoted "operational continuity" line, a "manual validation step" line and a Karmis email quote were not on the pages cited; one researcher spelled MacDonald as McDonald and another gave Karmis a stale title. Asking researchers to tag each URL (OPENED) or (SNIPPET) made the re-open pass much faster; keep that in the prompt.
- DEFECT FOUND AND FIXED in the shared builder `motions/shared/execution_kit/build_shelf_generic.py`: a build of named slugs rewrote `shelf_manifest_<rep>.json` with ONLY those slugs, dropping every other account. It now merges. Frank's manifest was restored by a full rebuild (7 shelves, 21 pages). Keegan's and Alex's manifests may have been trimmed the same way by earlier single-slug builds; check before trusting them.
- Strategy read given to Dallas, from the Sep 20 export: 83 records sit before the 12 Minute Meeting (82 past 45 days, median age 233 days, Five9 holds 45), 37 sit AT 12 Minute Meeting (36 past 45 days), only 1 early-stage record was created in the last 45 days. Deliver-first serves the trickle of new crossings and fires after the conversion Haresh's benchmark measures. Gaps named: trigger one stage earlier, a ranked pass over the parked stock, routing to the Intradiem AE on the record (Simon Bland 10 and James MacDonald 9 of the 12 Minute Meeting records; neither is in the channel), a measurement loop from stage history plus the deliver manifest, and detection latency (closed by the Salesforce report subscription once its first email is ingested). Not built.

**Sep 21 2026 night, stock rank + AE routing (dry run, no package built, nothing posted):**
- `automation/partner_stock_rank.py` ranks every record parked at a stage (default Delivered 12 Minute Meeting): best war-room signal, signal recency, then record age with the YOUNGER record first; customers rank last. Log `automation/logs/partner-stock-rank-<date>.md`. First run: 37 records, only ONE carries a signal (BCD Travel), because the partner slice joined the war room on Sep 21. Until a week or two of sweeps accumulates, the ranking is an age ranking. By AE: Simon Bland 10, James MacDonald 9, Mike Regan 4, Keegan Sanders 3, Jean-Anne McMahon 3, Rachel DiBello 3.
- DEFECT FOUND AND FIXED: the war-room log's signal line has had three shapes in a month and the LIVE poster parsed only the newest. One tolerant parser now lives in `automation/war_room_signals.py`; the poster and the rank script both use it (19 logs and 266 signal lines readable, up from 2 logs and 13).
- CUSTOMER GATE LEAK FOUND AND FIXED: "Centrica-Front Office" did not match "CENTRICA PLC". The gate moved to `automation/customer_gate.py` (shared by partner_deliver.py and the rank script) and now compares CORE company names with legal suffixes and the partner's line-of-business tail cut off. It flags Centrica and AMEX among the 37. The three staged packages still pass. Assurant reads as a customer, as expected for a pilot account.
- AE routing: `ae_routing` block in partner_events.json, mode `name` (default) or `mention`, eight Slack ids stored. The channel is PRIVATE, so a mention only notifies an AE who is a member. Never DM an AE from this job. A misplaced inline comment briefly disabled the one-pagers link in the deliver post; caught in replay and fixed the same hour.
