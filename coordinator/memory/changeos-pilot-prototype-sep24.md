---
name: changeos-pilot-prototype-sep24
description: "Sep 24 2026: Derek session settled the side quest product (Intradiem adoption, accept rate + self-cure, playbook behind each flag, paid add-on independent of Engagement Hub/Pro; internal pilot on assistant + Copilot with data, Intradiem interface as mockup; other tools = pie in the sky); ChangeOS three-phase prototype built in motions/ai_adoption_sidequest/changeos/ on the roadmap-deck look; Nov 3 9:00 calibration with Derek"
metadata:
  node_type: memory
  type: project
  originSessionId: d937e8e6-de04-4cb7-b28c-971c069eef89
  modified: 2026-09-24T15:18:06.900Z
---

**Derek session, Sep 24 2026 (Otter ScPnothj6rqU_rxB1tQo4cRvWYM, Kathryn + Dallas, Joey on PTO).** Derek's answers: the product is a daily or intraday view telling a manager where an agent is in the change cycle and what to do next; triggers and flags, then the playbook (questions to ask, conversation to tee up), which is the differentiator ("where we differ is the playbook that comes in behind that"). Data: the two adoption metrics already measured with every customer, session accept rate and alert self-cure, any product, front or back office; every tenant has it, nobody acts on it, "our reports are useless". Sold as a paid add-on to any Intradiem product, independent of Engagement Hub and Pro (Pro = burnout of the agent after the fact; this = adoption of the product before it); Hub feeds it once Hub ships. Not a replacement for the customer's own change program. Other tools via connectors/API: "pie in the sky", long-term stickiness (fits Matt McConnell's vision; Handle Time will erode), not first priority but not Intradiem-only either. By December: internal pilot with data behind it (internal assistant via Jason Dowden, Copilot) plus a mockup of the Intradiem interface that looks like Hub, another tab (Kathryn's and Dallas's framing, Derek agreed); Engineering will not allow building inside the product on an island, side quests build outside then get approved. Phase 1 vs Phase 2 framing endorsed; be careful with scope. Back office: few UPT live, no business readiness for BOO/QO yet. Customer friction lives with SMs (Joey), Faye (new CS AM from a customer) is a resource; ChurnZero AI later. Kathryn writes the playbook content (JSON fine). Second half of Jen Lee's doc (product screens, "now what do I do") is the useful half. **Calibration with Derek Nov 3 2026 at 9:00** (he is out two weeks, then travelling until Thanksgiving). Derek sent Jen Lee's "AI Change Management" doc (in ~/Downloads).

**Built Sep 24 2026:** `motions/ai_adoption_sidequest/changeos/` (model.js sample population: fictional customer, 4 teams, 144 people, seed 20260924; screens.js product screens incl. change-cycle + playbook; _tpl/deck.css + deck.js on the roadmap-deck look: forest, lime #A5CE38, cream mockups, Open Sans embedded; four .src.html pages; build.py --check; verify.js 39 checks; self-check panel `?` per page, 46 checks green headless; Prototype_Brief_ChangeOS.md; PREMORTEM_Sep24.md team-only). Routes `/`, `/phase-1/`, `/phase-2/`, `/phase-3/`; deployed Sep 24 2026 with --force: https://changeos-pilot.pages.dev (/, /phase-1/, /phase-2/, /phase-3/ all 200, noindex, byte-identical to the deploy folder `~/Desktop/Intradiem Deliverables/deploy-changeos-pilot/`). A first `pages project create` without --force fell into the wrangler trap and left a stray Worker `changeos-pilot` at changeos-pilot.andrewsdallas3.workers.dev serving the same files; not deleted (Dallas's call: `npx wrangler delete --name changeos-pilot`). Repo changes uncommitted. Phase 1 = find who fell out + playbook (by Dec 31, two faces); Phase 2 = inside the product, plays back into it, with Engineering (2027); Phase 3 = connectors, prediction, conversation layer (horizon). Restyled Sep 24 afternoon onto the real deck (https://allhandsroadmapslides.netlify.app/, Dallas sent it): cream paper slides for content and screens, forest dark for title, second and closing slides, Open Sans 600 headlines, orange eyebrow on light / lime on dark, marquee pill + orange rule + lime Contents button on titles, orange dots, section label bottom-left, contents pill and round arrows bottom-right. Exact tokens in [[product-allhands-deck-design-read-sep20]].

**Why:** Dallas asked for a pilot prototype on the roadmap-deck look, three links for three phases, with the premortem aimed at the outward-data problem; the Derek call inverted that problem (product = Intradiem rows the prototype has never touched).

**How to apply:** before Nov 3: Kathryn's questions per flag into ACTIONS in model.js, real internal rows through both adapters, and a written description of the accept-rate/self-cure export from Product or SM reporting (the premortem's one action). Never show Phase 3 alone; the Overview carries "real vs drawn". Related: [[side-quest-track-b-build-sep20]], [[side-quest-productized-offer-thinking-sep20]], [[product-allhands-deck-design-read-sep20]].


**Product cut, Sep 24 afternoon (Dallas: "I don't need slideshows anymore, I need the prototype product page, build out the phases").** Built `app.src.html` + `app.js` on top of the same model and screens: one app that looks like an Intradiem tool (forest rail, white top bar, DS v1 buttons and tables). Rail groups: Now (My team, Change cycle, Flags, Playbook, Executive view), Phase 2 with Engineering (Rules, Agent nudges, Insights), Phase 3 horizon (Predict, Sources, Closed loop, Simulate), Setup (Settings, Log). Top bar: source chips (internal assistant + Copilot = pilot, Intradiem = mockup with banner), team filter, phase switch (changes sources; future screens open with a "Drawn, not built" banner). Playbook is an editor (Kathryn types real content, persists, exports JSON); Settings edits thresholds (model.js CONFIG) and stage names live; Log is the audit trail; state persists in localStorage with Clear session. 13 self-checks green headless. Deployed with --force: https://changeos-pilot.pages.dev is now the product (Phase 1), `/phase-2/` and `/phase-3/` redirect into the app on that phase, `/overview/` carries the plan deck. Lesson: Dallas wants working product pages, not decks about them; decks are for the plan only.


**UX layer, Sep 24 evening (Dallas: not professional as-is; make it dynamic, enterprise grade, "make you not want to leave the page"; then "no need to tell me what's short, build it").** Added `ux.js` + the UX block in `_tpl/app.css`, loaded after app.js: insight strip on My team / Executive / Flags / Change cycle (animated adoption ring gauge, "N people fell out this week", conversations logged, back up, lowest team as a one-click filter, lime "Play the demo" button); person drawer (slide-in from the right on any person row: eight-week sparkline per source, stage chips, the flag with tee-up and questions, one-click Log the conversation, Phase 2 nudge and worked-example actions, history from the log); guided demo (`ChangeOSTour`, 7 steps on the real screens in Jen's wow-demo order: executive view, heatmap, change cycle, one person, log the conversation, fourteen days later, the loop; spotlight overlay + caption card; `?demo=1` autostarts); toasts on every action; motion (panels rise with stagger, rows slide in, KPI and gauge count-ups, bars and funnel grow from zero, hover lifts, drawer and scrim transitions), all honoring prefers-reduced-motion. Sparklines added to every person row. 18 self-checks (15 pass, 3 manual). Lesson: the product must carry its own story (guided demo) and a place to click into (drawer); tables alone read as static.

**Enterprise layer, Sep 24 evening (`ux2.js`):** Daily briefing view (Phase 1, second in the rail: "Good morning <manager>", three to talk to today with the opening question, team use per week, focus), search and command palette (top-bar Search button, Cmd/Ctrl+K or "/": people open the drawer, teams filter every screen, screens navigate), notifications bell with the open-flag count and a dropdown of flags, live activity feed on the executive view (seeded from flags and the log, one new event every 7 s while the view is open, stops elsewhere), first-run "New here? Play the demo" prompt (dismiss stored in localStorage), number keys 1 to 9 switch screens, view crossfade. 23 self-checks (21 pass, 2 manual). Deployed with --force to changeos-pilot.pages.dev.

**Momentum layer, Sep 24 late (Dallas: the vendor chips in the top bar had no rhyme or reason; anything that makes people ask "what's this for?" is a failure; the rail was flat; the logo was shrunken).** `ux3.js`: top bar now carries ONE source status pill ("1 source · Internal AI assistant", pulsing dot) that opens a real Sources screen (Phase 1 in the rail): unlock meter ("1 of 9 sources", names the next one to connect and why), three groups (The pilot: assistant + Copilot; The product: Intradiem, "Next · with Engineering", Request it; The rest of their stack: request-only until Phase 3), every card says what the source is for in the customer's words and what you gain; Connect runs a three-step animated flow (pull report, match roster, watching) then a payoff screen with a burst and count-ups (+N people measured, +N flags, sources watching) and "See the new flags" / "Connect X next". Phase switch never disconnects a source, only unlocks. Insight strip gained a "N of M conversations this week" progress bar with a celebration burst and toast when it moves. Rail rebuilt: real logo at 140x34, stroke icons per item, orange badges (open flags, today's three, sources), sliding lime active indicator, ambient gradient + dot grid, plain group names (Now / Next · Phase 2 / Later · Phase 3), numbered future items, a signed-in card (manager of the filtered team). 29 self-checks (27 pass, 2 manual). Deployed with --force. Lesson: every control needs a reason on its face; a connect flow must pay off instantly to make people want the next connection.
