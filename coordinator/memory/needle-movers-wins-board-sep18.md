---
name: needle-movers-wins-board-sep18
description: "Sep 18 2026: Needle Movers wins board LIVE at needle-movers-wins.pages.dev (deployed on Dallas's go); staging folder in ~/Desktop/Intradiem Deliverables/deploy-wins-board/; one page on the Rachel brand kit, five seeded wins, scope filter, data-driven so a new win is one JSON entry"
metadata:
  type: project
---

Built Sep 18 2026 as the fifth idea in Dallas's Needle Movers email to Jen Lee ([[spot-bonus-program-sep18]]), so it is ready to show if she says yes. STAGED ONLY: Dallas said do not deploy until he says go.

- Folder: `~/Desktop/Intradiem Deliverables/deploy-wins-board/` (index.html, intradiem-brand.css copied from brand-kit, assets/ with logo and background, _headers noindex). brand-check.py passes clean.
- Every win is one entry in the `wins-data` JSON block at the top of index.html (title, scope team/teams/org, builtBy, team, builtFor, constraint, built, impact, reuse, proofLabel, proofUrl, date). Counts and the scope filter compute from the data; a how-to comment sits above the block.
- Public-page wording on purpose: no partner names (3xG, SagesS3), no Cleveland Clinic renewal or RFP detail, save-room proof points at the save-rooms index. The email to Jen named those; a pages.dev page must not.
- Deploy when told: `npx wrangler pages project create needle-movers-wins --production-branch main --force` then `npx wrangler pages deploy . --project-name needle-movers-wins --branch main --commit-dirty=true --force`, and delete the `.wrangler` folder before and after (wrangler 4.135 routes to workers.dev without --force and uploads its own cache).

**Lesson:** the kit's `.section-brand` does not set heading colour, so an h1 inside it renders dark green on dark green and the linter does not catch it; always set `header.section-brand h1{color:var(--id-white)}` and check the header in a render.

**DEPLOYED Sep 18 2026 20:55 CT** on Dallas's go: https://needle-movers-wins.pages.dev (Pages project `needle-movers-wins`, noindex header confirmed, live file byte-identical to the local index.html). Changes made before deploy: every "Built by Dallas Andrews" row removed (builtBy is now optional, cards show Team only; zero mentions of his name on the page); the save-room card carries NO link ("Walkthrough on request from GTM Engineering"); third counter is scopes covered; footer tells others how to get on the board.

**Why the save-room card has no link, and an OPEN exposure:** https://save-rooms.pages.dev/ (the ROOT) is the internal Sep 10 "Churn-Risk Save Plan" read, marked "Prepared for Dallas, internal read", naming three at-risk accounts and carrying frank lines about the AM process. The Needle Movers email to Jen links to save-rooms.pages.dev/cleveland-clinic/inger/, so anyone who trims that URL lands on the internal read. NOT fixed here on purpose: the deploy-save-rooms folder holds Keegan execution-kit work that is staged and not approved to go live, so a redeploy from it could publish that. Fix needs its own pass: swap the root for a neutral index, confirm what else in the folder is staged, then redeploy.

**Gotcha:** checking a new pages.dev hostname BEFORE the project exists makes the local resolver cache the miss; afterwards curl returns 000 "could not resolve host" for many minutes while the site is fine. Verify with `dig @1.1.1.1 <host>` and `curl --resolve "<host>:443:<ip>"` (quote it, zsh does not split an unquoted variable). Also the first `pages project create` can fail transiently; rerun it before deploying.

**Save-rooms exposure, STAGED FIX Sep 18 2026 ~21:30 CT (not deployed, waiting on Dallas's go):** file-by-file check showed all 70 files in deploy-save-rooms byte-identical to live, so NOTHING was staged-but-unapproved; the Keegan kit went live that afternoon (the index hook saying "staged" was stale, the memory body said deployed). Found a second, worse exposure: /cleveland-clinic/ served the INTERNAL save room ("5 of 7 signals red", owners proposed), one URL-trim from the /cleveland-clinic/inger/ link sent to Jen. Staged: neutral root "Account rooms" page with NO account links on purpose (a link list would turn one shared room link into access to every account's room and its contact emails); /cleveland-clinic/ now redirects to /cleveland-clinic/inger/; 404 no longer says "the save plan". Internal pages moved to ~/Desktop/Intradiem Deliverables/Save Rooms - internal (local only)/ with a live-backup-2026-09-18 subfolder (exact bytes that were live). Only build_save_room_inger.py writes into the deploy folder on its own, so no script re-copies the internal pages; never hand-copy an internal read into a deploy folder again. Deploy command when approved: from the folder, delete .wrangler, `npx wrangler pages deploy . --project-name save-rooms --branch main --commit-dirty=true --force`, delete .wrangler, then verify root, /cleveland-clinic/ and three account rooms.

**Save-rooms fix DEPLOYED Sep 18 2026 ~21:45 CT on Dallas's go** (deployment 1da329a2; 6 files uploaded, 67 unchanged). Verified live with four cache-busted samples per path, all byte-identical to the staged files: root (neutral "Account rooms"), /cleveland-clinic/ (redirects to /cleveland-clinic/inger/), Inger, Alex (Elevance) and Keegan (Citizens) rooms, brand css and logo; unknown paths return a true 404 with the new wording; zero hits for internal-read language on root or /cleveland-clinic/; noindex header intact. RESIDUAL: Cloudflare Pages keeps every earlier deployment reachable at its own <hash>.save-rooms.pages.dev address, so the old internal pages still exist at those unshared, unindexed hash URLs until the old deployments are deleted (`wrangler pages deployment list/delete --project-name save-rooms`); deleting them removes rollback, so it waits for Dallas's call.

**Old save-rooms deployments DELETED Sep 18 2026 ~21:10 CT on Dallas's go:** 29 older deployments removed (the list pages at 25, so loop list-then-delete until only the kept one remains), only 1da329a2 left, live site and all rooms still 200. 28 of the 29 old hash addresses went 404 within minutes. ONE DID NOT: https://5b56c4d0.save-rooms.pages.dev still returned 200 with the internal plan at / and the internal Cleveland Clinic room at /cleveland-clinic/ 25 minutes after deletion (last check 21:31 CT), while the API says that deployment id does not exist (code 8000009), cache-control max-age=0, served from DFW. Reads as Cloudflare edge lag on one deleted deployment; nothing more can be done from the CLI. The address was never shared and is not indexed. NEXT: recheck it; if it still answers the next day, open the save-rooms project in the Cloudflare dashboard to confirm it is absent there and raise it with Cloudflare support.
