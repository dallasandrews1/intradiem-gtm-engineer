---
name: intradiem-cohesion-layer
description: "The GTM Engine cohesion layer Dallas + Claude built (Jun 24 2026) — connective tissue tying his point-tools into one motion, incl. the Clay Build Pack"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6350bfb3-4c97-47b2-b5e2-deab90936691
---

Built Jun 24 2026 in project folder `gtm-cohesion-layer/`. Fixes the gap that Dallas had strong separate engines/skills/dashboards but no connective tissue. All runs dry-run/seeded pre-access; goes live Jul 6 by wiring TODO hooks (no structural change).

**Files:** `engine_state.json` (single source of truth, all dashboards read it — fixes drift); `Clay_Build_Pack.md` (click-by-click Clay build below GTM_Engine_Build_Spec: tables/columns in order, enrichment waterfall, fit/intent/grade formulas, signal columns, source-tag writes, webhooks — Day-1 rebuild in ~2hrs); `Attribution_Loop_Spec.md` + `attribution_dashboard.html` (reply-sync agent + live two-motion funnel = the credit-war keystone); `Weekly_Conductor_Runbook.md` + `conductor.py` (7-stage orchestrator = "10x as one weekly push", verified all gates fire); `approval_queue.html` (human approve/edit/reject guardrail); `deliverability_monitor.html` (premortem P1 send-gate); `_README_Cohesion_Layer.md` (index).

**On Clay:** can't provision live Clay pre-access (no access til Jul 6; Clay table config lives in Clay UI, not a connector). Right pre-access move = the Build Pack spec, done. Don't promise live Clay table creation before start.

**Consolidation (Jun 24 2026):** Auto-run scheduled — `weekly-gtm-conductor` task runs conductor Mon 8am, reports gate blocks (joins existing weekday-7am `daily-signal-scan`). Artifact cleanup: **archived** `intradiem-gtm-signal-engine` + `morning-intelligence` (interview-era, superseded) to tombstone stubs — safe for Dallas to delete from sidebar. **Merged** control_tower's 4 control tiles (approval-queue gate, deliverability gate, variant winner, attribution credit) into the **`command-center`** live artifact = the SINGLE front door; command-center stays live on intradiem-tam/intradiem-signals MCPs, controls run on a seeded snapshot until go-live. So control_tower.html is now the local/dev twin; command-center is canonical. Live artifacts kept clean (own distinct jobs): golden-list (account board), control-plane (install-base white-space=200 back-office), roi-calculator, dallas-mission-control (personal tracker), intradiem-day1-audit-console. Two data planes by design: live artifacts via MCP vs cohesion windows via engine_state.json.

**Loop-engineering hardening (Jun 25 2026) — gaps 1–4, both data planes synced:** Pressure-tested the motion against the "loop engineering" discipline (Anthropic eng docs + Osmani article Dallas shared). Four ADDITIVE upgrades, no existing work touched (verified: all 12 engine_state keys + 18 conductor functions + all guardrails/variants intact; zero artifacts harmed; control_tower untouched). (1) **Cost per accepted change** — `draft_quality` block + `compute_draft_quality()` rolls `approval_queue.resolved_ledger` into clean_accept vs edited_then_approved vs rejected; below 0.5 floor = draft engine is the bottleneck, not the gate; surfaced as a tile on approval_queue.html. (2) **Independent critic (Stage 4b)** — `stage_critic()` objective gate (deliverability, ICP/persona, in-scope motion, verified-claims regex, length) holds bad drafts BEFORE the human queue; maker≠checker; held items render with reasons. (3) **Spec reread (Stage 0)** — `stage_spec_reread()` + `north_star` block hashes GTM_Engine_Build_Spec.md each run, logs drift, re-asserts single in-scope motion (anti-drift). (4) **Security cadence** — `security` block + `check_security()` (30-day permission re-audit, log-sanitization, skill-source policy) + new `security_audit_current` preflight blocker (now 4 seed blockers, not 3). Files changed: conductor.py, engine_state.json, approval_queue.html, _README, Weekly_Conductor_Runbook. **CRITICAL two-plane note:** because live artifacts can't read engine_state.json, the engine-plane changes don't auto-flow to the canonical front door. SYNCED command-center artifact additively (added Critic gate + Draft acceptance tiles; EX-002/EX-003 shown critic-held; pending 2→1; 4th blocker) so both planes tell one story. Seeded held example: EX-002 (Back Office) is held as out-of-scope per WIP-of-one — correct behavior, kept by Dallas's choice. Naveen framing: "maker → objective checker → human; I automate around the judgment, not the judgment itself."

**Gate design (the guardrails ARE the product):** measurement before volume, ICP before sourcing, human before send. Conductor is fail-closed; Stage 5 human gate; Stage 6 deliverability-gated. See [[intradiem-q3-mandates]], [[intradiem-product-scope]], [[mission-control-dashboard]].

**Premortem guardrails added (Jun 24 2026):** After a strategic-premortem on Dallas's day-one approach, wired three self-policing tripwires into `engine_state.json → guardrails`, recomputed every conductor run. (1) **WIP-of-one** — `check_wip()` stage 0, `violation=true` if >1 motion `scaling` (anti-sprawl, premortem Failure #3); star_ratings is the one scaling motion, everything else (incl. back_office + standing modernize-OKR items) in backlog. (2) **Throughput-by-operator** — stage 8 `stage_guardrails()`, `alarm=true` if Dallas's share of weekly releases >20% ceiling (= operable-by-Dallas bottleneck, Failure #1). (3) **Operator-test** — motion flips to `operable` only after 2 consecutive batches released by a non-Dallas operator; every Dallas release resets the streak. Surfaced on control_tower.html (3-panel guardrail row) + approval_queue.html (`released_by` operator selector + owner-warning + gate banner). Core principle encoded: Dallas = owner who designs the motion, team = operators who run it; "done" = team runs it unaided, not "works when Dallas runs it." All verified (WIP block, bottleneck alarm, operable flip all fire; both dashboards' JS compiles).

**Production-integrity hardening (Jun 24 2026, "real ENT org, no errors" pass):** Fixed two SILENT-failure bugs. (1) Owner now matched by stable `is_owner` flag (+ `owner_id:"dallas"`, `id` on each operator), NEVER by display-name string — a name-string match fails OPEN if prod `released_by` is SSO/email (owner releases miscounted as team → alarm never fires). `_owner_releases()` requires exactly one is_owner; zero or >1 → `integrity_error` + alarm FORCED on (fail-loud/conservative). (2) `roll_weekly_window()` (live-only, gated on `not DRY_RUN`) snapshots to `last_week` + zeroes `released_this_week` weekly via `window_start`; without it weekly share silently became all-time. approval_queue `isOwner()` now reads the flag too. README live-wire checklist got 5 go-live integrity steps incl.: `released_by` must be real human/controlled-vocab; operator-test streak authority MOVES to conductor weekly roll at go-live (per-item HTML increment is PREVIEW-ONLY, would overcount in prod). 5 integrity unit tests pass (flag-match w/ email name, zero/two flags fail-loud, weekly reset).

**Portfolio-wide silent-failure audit (Jun 24 2026):** Audited all engines + 5 dashboards. Verified cross-module contracts MATCH (conductor reads tam `icp_total`/`fresh`, signal `signals_fired`/`primary_routing`/`company`, impact `net_new`/`install_base`/`realized` keys — all correct). Fixed 4 silent-failure classes: (F1) Stage 6 send now asserts every approved item's `released_by` resolves to a known operator (name OR id) else RuntimeError → fail-closed abort, live-only. (F2) measurement-before-volume ENFORCED: `mark_degraded()` + if Stage 2 rescore or Stage 3 signals degrade, Stage 4 holds to test slice + sets `approval_queue.volume_blocked=true`; degraded runs flagged in `last_run_status` as "— DEGRADED (stage)". (F3) `impact_engine.build()` now returns `sources`{tam_plays,signals,outcomes} + `data_complete`; conductor degrades + logs BLOCK if any upstream export missing (was silently returning $0). (F4) all 5 dashboards (approval_queue, control_tower, attribution, deliverability, variant_tracker) show red "could not load engine_state.json — placeholder, not live" banner on fetch failure instead of silently rendering seeded zeros. All verified: F1 aborts on blank/unknown + passes on valid; F2 blocks volume; F3 flags missing; existing unit tests still 21/21 + 14/14; all 5 dashboards' JS compiles; seed dry-run healthy. Documented in runbook "Silent-failure elimination" section + README go-live checklist.

**Preflight go-live gate (Jun 24 2026):** Added `python conductor.py --preflight` (argparse CLI) — runs 10 assertions: state_structure, owner_identity, attribution_ratified, baseline_set, deliverability_green, tam/signal/impact engine CONTRACTS (actually calls each engine + validates the keys conductor reads), wip_sane, approval_attribution. Prints PASS/FAIL report + verdict, exits nonzero if not ready. `run()` calls preflight automatically when DRY_RUN=False and REFUSES to execute (fail-closed, no stages run) until all pass — live operation is physically gated on green preflight. Verified: seed → NOT READY (3 blockers: attribution_ratified, baseline_set, deliverability_green = the real Jul-6 to-do list), exit 1; fully-ready state → READY + live run executes end-to-end; unready+live → blocked before any stage. These 3 seed blockers ARE Dallas's go-live checklist.
