---
name: one-system-fallback-decision-aug2
description: "Aug 2 2026 decision: Daily Action Brief is the only rep-facing delivery method in both funding worlds; Lemlist is the active source, manual method parked as fallback-only; register + reversion runbook in motions/shared/Manual_Fallback_Register_Aug2.md"
metadata:
  type: project
---

Aug 2 2026, Dallas's call after realizing the pre-trial manual quarterbacking overlapped Lemlist: run ONE system, not two. The Daily Action Brief (spec v2.4) is the only rep-facing delivery method whether or not the Lemlist purchase is approved (~Aug 13 decision, trial ends ~Aug 13). Lemlist stays the system of record and executor; the brief is the coaching/accountability surface over it (reply drafts in rep voice, call branch maps, DONE/SKIP/HOLD loop, full-copy transparency). If funding fails, only the composer's data source changes (Lemlist API → staged task lists from the motion sequence docs); reps see the same format and channels minus the autopilot section.

Parked as fallback-only, never deleted: the manual daily-blitz playbook delivery and the strike-room 10-day manual cadence choreography. The copy/playbook docs themselves stay canonical sources in both worlds. Register + reversion runbook: motions/shared/Manual_Fallback_Register_Aug2.md; the spec's SOURCE AND FALLBACK section forbids building a second rep-facing method.

ELT visibility comes from receipts, not museum artifacts: Friday readout + pipeline-receipts package trial usage (relay logs, thread-ingest DONE rates, Lemlist stats, meetings) into the funding case. Go-live speed compounds that case, hence the Aug 3-4 Start runway. Related: [[action-brief-format-v2-aug1]], [[cold-call-contract-aug2]], [[lemlist-trial-gameplan-jul31]], [[lemlist-trial-build-jul31]].
