---
name: backoffice-dedup-broken-lookup-jul26
description: Jul 26 2026 - chased a "broken lookup" theory on BO_People_Master's seed-dedup that turned out WRONG; a full manual name-key diff proved the 313 net-new contacts are genuinely clean (zero overlap with Savannah's seed), and the 4 lists shipped to Mary Ann same day
metadata:
  type: project
---

**CORRECTED CONCLUSION (Jul 26, end of day):** everything below about a "broken table reference" was a wrong turn. Dallas confirmed in the Clay UI the lookup's table picker was correctly pointed at Existing_BO_Campaign_Seed the whole time (the API/MCP read displaying the table's name instead of a raw ID was just how the tool renders a resolved reference — not proof of a broken string). The real explanation for 0/313 matching the seed: **there is no bug.** A full manual diff (all 313 vs all 214 Normalized Name Key values, hand-checked for near-misses at every one of the 11 shared accounts) found genuinely zero overlap — Savannah's seed is a broader install-base campaign list (only 60 of 214 rows are even at these 15 Tier-1 accounts) built from a different sourcing method (CRM-curated ops contacts) than this pull (Clay title-search across 4 personas). Different people at the same companies, not a matching failure. The 313 net-new figure was accurate the whole time. **Lesson: when a dedup shows an implausible 0% or 100% rate, verify with an independent manual diff before assuming the mechanism is broken — Clay's own "No Record Found" can be a genuine completed result, not a stuck/unrun state, and don't re-diagnose a config a human has already visually confirmed correct.**

**Cleanup (Jul 26, same day):** Dallas renamed the whole Back Office Motion workbook out of internal jargon into human-readable table names (e.g. `BO_People_Master` → "Back Office Contacts - Master List", `Existing_BO_Campaign_Seed` → "Savannah's Existing BO Contacts", `BO_ops_leader_v2` → "Operations Leaders (Claims & Back Office)"). Also deleted the pre-cap-raise orphaned duplicates `BO_ops_leader` (60 rows) and `BO_exec_coo` (45 rows) — confirmed zero downstream dependents (Master List's only imports are the 4 live `_v2`/current tables, math checked exactly against the recorded gross-317 figure). Also deleting `BO_Sample_People` (111 rows) + its 5 attached source searches — identified as the ORIGINAL wrong-buyer claims-ops pull from before Mary Ann's Jul 23 correction, fully superseded, single-consumer sources so safe to remove together. Standing pattern for future motions: after a build stabilizes, do a jargon-to-plain-English table rename pass and prune orphaned pre-iteration tables — Clay has no delete API, this is always Dallas's hands.

Jul 26 2026: picked back up the open loop from [[backoffice-live-build-state-jul23]] (the queued Normalized Name Key fix, gated on Rich Turner/Melissa Foy/Jeanmarie Morrison landing as seed-dups before shipping to Mary Ann Chandler). Read-only Clay check (table `t_0tingaqoNzymfmSpEZh` = BO_People_Master, workbook `wb_0ti4jh8ATmjiCowc7JM`) found the problem is worse and different than the Jul 23 whitespace theory:

**Root cause confirmed:** the `Lookup Single Row in Other Table` action (field `f_0tinokdSbe628qPxU5C`) that feeds `Seed Contact Match Type` has its "Table to Search" input set to the literal text `"Existing_BO_Campaign_Seed"` — a table NAME, not a table ID. Clay's own docs (university.clay.com/docs/lookup-rows) confirm this field requires an actual table ID. Because it's unresolvable, the lookup returns "No Result" on all 313/313 rows, and every row defaults to `net_new`. This is a wiring mistake, not a data-quality issue — Rich Turner/Melissa Foy/Jeanmarie Morrison also don't exist as ROWS in BO_People_Master at all (never re-sourced by the persona pulls), so the original 3-name test can't even run until the reference itself is fixed and re-tested against whichever seed members DID get re-found.

**Current BO_People_Master state (all still true as of Jul 26):** 313 rows, 0 flagged DUP_IN_SEED or NEAR_MISS (100% net_new — unsafe to trust). Persona split: ops_leader 105, exec_coo 90, bo_product_owner 63, bo_claims_it 55. Work Email/ZeroBounce verification only 76.4% complete (239 valid / 313; 61 null, 10 invalid, 2 unknown, 1 abuse). No sort/grouping by account→persona→seniority despite `Role Mapping Number` + `Seniority Tier Rank` fields existing for exactly that purpose (looks like raw append order from the 4 persona source tables, never sorted). No export/Salesforce-push evidence — confirmed nothing has shipped.

**Fix required (Clay UI, table-structure edit, needs Dallas's hands — not agent-buildable):**
1. Get Existing_BO_Campaign_Seed's real table ID (still unknown — grab from its Clay URL when open).
2. On BO_People_Master, open the Lookup Single Row in Other Table action feeding Seed Contact Match Type; replace the Table to Search value with the real ID (Target Column/Filter Operator/Row Value are already correctly wired to the Normalized Name Key).
3. Also confirm Existing_BO_Campaign_Seed actually has a populated Normalized Name Key column matching `f_0tino65Hrbko94Q4Ae4` (same lowercase+strip-punctuation formula) — never verified, since the seed table ID was unreachable via API (Enterprise-gated `clay tables list`).
4. Re-run the lookup across all 313 rows (internal table lookup, no external provider — expect ~0 credit cost, but glance at `clay credits` before/after).
5. Re-verify (read-only Clay check): real match rate against the 214 seed, spot-check any known overlaps land as DUP_IN_SEED, confirm net-new counts per persona are trustworthy before anything goes to Mary Ann.

**Seed table verified sound (Jul 26):** Existing_BO_Campaign_Seed = t_0tin1p6p4q7rmi6xY78, 214 rows, its own Normalized Name Key (f_0tino65Hrbko94Q4Ae4 = `({{First Name}}+{{Last Name}})?.toLowerCase()?.replace(/[^a-z0-9]/g,"")`) is populated and equivalent to BO_People_Master's key — Rich Turner/Melissa Foy/Jeanmarie Morrison all present with correct normalized keys. The seed side is not the problem; the broken table-ID string on BO_People_Master's lookup action is the sole confirmed defect. Secondary risk flagged for post-fix spot-check: 3 seed rows carry nicknames/maiden names in parens ("Michael (Mike) Gold", Michael "Mike" Yoest, "Rosemary Kenyon (Baia)") baked into their normalized key — will only match BO_People_Master if that table's Full Name field carries the same parenthetical, otherwise still false-net-new.

**Confirmed structurally UI-only fix, ties to [[built-means-in-the-live-tool]]:** checked `edit_node` (scoped to workflow nodes only, no tableId/fieldId param), `execute_clay_action` (preview/test only, no save), `table` MCP tool (schema/query read-only by design), and `clay` CLI's `tables update` (only toggles query-enabled flag). None can write a native table's action-column config — swapping the Table-to-Search value on a Lookup Single Row action is Clay-UI-only, no MCP/CLI write path exists for it at all (not just for this table, structurally for any table).

The existing follow-up draft [[BackOffice_Followup_Email_MaryAnn_4Lists]] (Intradiem GTM Engineer project folder) is ready to fill in and send once this passes — its own send gate already said not to ship until the dedup checks out, which turned out to be the right call. See [[backoffice-icp-maryann-reframe-jul23]], [[backoffice-live-build-state-jul23]].
