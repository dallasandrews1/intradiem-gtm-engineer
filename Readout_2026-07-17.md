# Weekly Readout, Jul 17 2026 (Week 2)

Draft for Dallas to review and send. Nothing here has been shared with Naveen.

## 1. BLUF

Week 2 turned the single Stars motion into a reusable engine: seven shared Clay Functions now back every motion, a second motion (Cost-Mandate) is built end to end on a proof slice, and two one-off strike rooms shipped against Nathan's 12-account list. The thing that needs your attention is unchanged from Week 1 and now compounding: sends still have not opened, the mailbox warmup and the two Week-1 ratifications (baseline, attribution) are still the gate, and every week they stay open compresses the runway to the Sep 19 tripwire.

## 2. Leading indicators (engine outputs, this week)

Week over week is against the Jul 10 readout (committee sourcing complete, zero sends).

Reusability layer: seven shared Clay Functions built and live (eligibility, send-ready, draft-clean, email-verified, persona-key, tokens-ready, draft-critic). This is the piece that makes the second and third motions cost hours instead of weeks. One real leak found and fixed in the audit (a hardcoded install-base clause in tokens-ready); the other six are clean.

Second motion stood up: the Cost-Mandate universe is live in its own workbook with fit scoring, intent scoring, and the customer kill-switch verified firing (four install-base names forced to excluded, six eligible). The L3 contact slice ran on the five eligible accounts. Honest result: the H1 re-sourcing test proved four of those five do not actually fit a "cite their own cost number" motion (stale, growing, or wrong division); only Nike survived clean. The fail-closed engine refused to fabricate a number, which is the design working. Next is sourcing the right accounts, not forcing these.

One-off strike rooms: Blue Shield of California and Aflac (accounts 1 and 2 of Nathan's 12), each with a full buying committee sourced and email-validated, drafted and ready for his walkthrough.

Signal layer: the L2 intent layer is live and wired on the real table (hiring-cluster and leadership signals firing into an intent score that self-cleans and gates on customer flag). War room ran all five business days. Ninth consecutive cycle with zero fresh outbound Priority 1, which is a real read on the universe right now, not an idle engine.

Stars Wave 1: four broken drafts repaired in place, both campaigns ungated (still in Draft, nothing launched), and a critic PASS guard added to the sync condition so nothing syncs to a campaign without clearing the gate.

Sends, replies, meetings sourced: 0 / 0 / 0. Still a gate status, not a shortfall. The funnel counts in engine_state.json are genuinely zero; the variant-performance and approval-queue numbers in that file are seeded EXAMPLE rows (flagged example: true) and are not reported here as real.

## 3. Lagging indicators (realized only)

Realized this week: nothing. No meetings sourced, no pipeline, no revenue. outcomes.csv still holds only the two pre-start June entries (one discovery meeting, one $180K opp from demo-era data); they predate the role and do not count toward Q3. The realized column starts moving when sends do.

The $41.6M "opportunity surfaced" figure in impact.json rides the placeholder ROI model on a Jun 13 build-phase snapshot. It is stale and not Intradiem-verified, so it is not a claim in this readout. Surfaced gets re-cut from live data once the attribution fields exist.

Note on inputs: engine_state.json was last written Jul 13 and impact.json Jun 13, so both trail this week's build. Since the realized numbers they carry are still zero or pre-start, the staleness changes nothing in this readout, but it is why the Week-2 build detail above comes from the ledger and war-room logs, not from those two files.

## 4. Gates and tripwires

- Attribution contract: still not ratified in writing; gtm_engine_sourced fields still absent in Salesforce. New this week: the Jul 16 Clay/Salesforce call scoped the path, and the custom fields route through a Salesforce case to Eric's team on two-week sprints, built by Genna in sandbox if bandwidth allows. That puts the attribution timeline partly outside my control, which is worth your eyes now.
- 1x baseline: still not set, so the 10x frame still has no denominator. Carried from Week 1.
- Deliverability: no mailbox OAuth, no warmup started. Both still precede any launch. The engine_state deliverability block is example domains last checked Jun 24; the real inbox path has not started.
- Security cadence: the permission audit is overdue in engine_state (no audit logged yet, log-sanitization off). Not launch-relevant this week, flagged so it does not surprise the preflight later.
- Day-75 tripwire (under ~10 of 15 sourced meetings by Sep 19 escalates): 0 of 15, sends not open. Nine weeks out. The arithmetic from Week 1 holds and tightens: warmup takes weeks, so the gap between first send and Sep 19 shrinks every week Wave 1 stays gated.

## 5. Credit stewardship

Roughly 864 of 5,000 consumed month to date (about 17%), verified against the Clay Usage page and logged to the append-only ledger. Budget model clarified this week: the 5,000 is a monthly reset, not a quarterly cap, which is more headroom than the Week-1 framing implied. This week's spend went to the L2 signal layer fires (hiring cluster ~88, leadership research ~106, job-openings sample ~20), the Cost-Mandate proof slice (~35 plus the L3 contact waterfall), and the two one-off strike rooms plus ZeroBounce sweeps (single digits each). Projected steady state is ~520/month at the parent-grain L2 cadence, so the 3,000 mid-burn flag is not in play at plan. Every paid run carried a pre-run estimate; one drift note logged (L2 hiring ran at contract grain, 88 vs ~52 estimated) with the parent-grain fix specced.

## 6. Signals and plays of the week

The Stars recalculation fight is now an industry-wide litigation wave: Elevance ($115M), SCAN ($125M), and Alignment ($50M) all suing CMS for the same Clover recalculation logic, about $290M publicly priced by the plaintiffs themselves. That hands the copy skills the strongest reframe this motion has had, and it is the industry's own language, not ours: even a plan that wins the ratings-math fight still has to move its actual CS measures, and the courtroom fixes none of them. UnitedHealth beat and raised on Jul 16, which usefully kills any "MA margins are cratering" macro premise; the durable angle stays account-specific (a contract's own sub-4.0 CS star and the QBP dollars on it). Elevance (reports Jul 22) is the sharpest single-account application to stage next.

## 7. Next week, committed

- Re-source the Cost-Mandate universe against the H1 finding: signal-first accounts that actually have a cited cost-takeout number, replacing the four that failed the fit test.
- Continue Nathan's 12-account one-offs (targeting one to two more strike rooms).
- Stand up the parent-grain L2 helper table and wire the free measure-slippage signal, then run the first pre-estimated full sweep.
- Fold the Elevance contract-geography detail into the earnings-reaction touch ahead of its Jul 22 print.
- Keep the mailbox OAuth and warmup as the top unblock ask, since warmup time is the long pole to first send.

## 8. Risks flagged early

- First send vs the earnings window and the Sep 19 clock. The freshest forcing functions of the quarter are firing now (the litigation wave, the Jul 16 to Aug 5 earnings stretch), and the engine cannot act on them until warmup and the ratifications clear. This is the same risk as Week 1, one week more expensive.
- Attribution timeline now has an external dependency (Eric's team sprints, Genna's sandbox bandwidth). If that slips, sourced meetings still have no system of record even after sends open. Flagging so it is co-owned, not discovered late.
- WIP discipline. Stars is the designated scaling motion, yet meaningful build went into Cost-Mandate and the one-offs this week. Defensible as reuse-layer and pipeline-coverage work, but worth your read on whether that split is the right call while Stars is still pre-launch.

## 9. Asks

1. The two Week-1 ratifications, still pending your read: the 1x baseline (so 10x has a denominator) and the attribution contract (so sourced meetings have a system of record). Both are the actual gate on reporting progress the way we want to.
2. Your read on the mailbox and warmup path. It is the long pole to first send and the only thing standing between the built engine and the Sep 19 arithmetic.
3. Your read on the Stars-vs-Cost-Mandate split: hold the line on one scaling motion, or keep building the second in parallel given the reuse layer makes it cheap.

---

## Slack version

Week 2 close: the single Stars motion is now a reusable engine. Seven shared Clay Functions live (motions now cost hours, not weeks), the Cost-Mandate motion built end to end on a proof slice, two one-off strike rooms shipped for Nathan's list (Blue Shield of CA, Aflac), L2 signal layer wired. War room ran all 5 days.

Sends, replies, meetings sourced: still 0 / 0 / 0 by design; realized column stays empty and separate from surfaced. Honest note: the Cost-Mandate H1 test proved 4 of 5 first-pick accounts do not fit; only Nike survived and the fail-closed engine refused to fabricate. Re-sourcing next week.

Risk, carried and compounding: first send still gated on mailbox warmup plus the two unratified Week-1 items, and Sep 19 is nine weeks out.

Asks: your read on the 1x baseline and attribution contract (both pending from Week 1); the mailbox/warmup path; and whether to hold one scaling motion or keep building the second in parallel.
