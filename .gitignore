# OS and editor junk
.DS_Store
.~lock.*#
*.tmp
_wtest.txt

# Python
__pycache__/
*.pyc

# Node
node_modules/

# Secrets and local env (none committed today; keep it that way)
.env
.env.*
*.pem
*.key

# Logs and transient runtime state
logs/
*.log
gtm-hosted-platform/brain/logs/
intradiem-signal-engine/data/suppression_state.json
intradiem-signal-engine/data/notified_state.json

# Claude Code local settings
.claude/settings.local.json

# Packaged deliverable bundles (regenerable; keep the sources, not the zips)
*.zip

# Personal-file guard. Job-search era docs were moved out of this workspace
# (2026-07-01); these patterns keep any future strays out of the repo.
*[Rr]esume*
*nterview*
*[Ss]alary*
*egotiation*
_archive/zi7Zjxcl

# Local secrets and env for the automation swarm (LEMLIST_API_KEY lives here)
automation/config/*.env

# Tool caches and editor backups
.wrangler/
*.bak

# Stray upload/snapshot zips that landed at the root (Jul 9 coordinator snapshot)
ziltSwKH

# Superseded Jul 9-15 AirDrop migration pack (contains interview-era memory copies);
# the versioned coordinator/ folder replaces it. Relocate out of the workspace.
work-migration-pack/

# Personal, never work-repo material
*TAIS*
linkedin-about*
